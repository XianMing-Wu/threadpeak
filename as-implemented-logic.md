# ThreadPeak 现状、目标与验收（2026-09-07）

用户最新裁决高于旧稿。[Agent 合同](agent-specs.md)记录提示词与上下文；[工程执行记录](docs/backend-rebuild-2026-09-06.md)记录技术选择和研究依据。[改造前清单](docs/archive/as-implemented-logic-before-learning-v2.md)仅供迁移追溯，旧 G1/G2、批注和内存持久化不再是目标。

## 1. 本次源码实际

- 2026-09-07 目标访谈重构：默认 durable 管线采用 goal-v1。R2 保存目标假设、建议的适用条件和证据、候选概念用途；R3 每题三个建议加独立自定义输入，追问不再必然换题。用户原话和全部真实补充进入 R4，R4 画像的动机、基础、限制与非目标逐字回查用户原话，未知个人特征不填成假设；R4 必须输出最终目标及每概念的用途、深度、检验任务和资料锚点；程序回查实际 F 内容并编译来源绑定。`learning-goal.ts` 从本账号已发布路线构造学习目标，首次搜索/筛选/三角度/讲解/追问/请教均携带，新对话保留该目标。旧无目标资料不补造。相关决定及质量样本见 [目标重构记录](docs/goal-aligned-agents-2026-09-07.md)。
- 压缩保护用户目标、实际答案、用户消息及当前概念；按语义边界分块且保留每个字符，按来源独立摘要，缓存区分用途、窗口和合同版本。兼容压缩删除了截原文与合并同作者证据的兜底。不可压缩骨架超窗仍需分区，当前会保留任务并失败，不宣称任意长度都能处理。

- 默认服务入口 `server/main.ts` 接 `server/durable/bootstrap.ts`。旧 orchestrator 保留为迁移参考/隔离测试，默认服务不注册旧的首轮、G1/G2、批注写入口。
- `server/durable/database.ts` 提供 PostgreSQL/PGlite 表，任务、检查点、资源、事件、会话令牌、作者关系和 provider 并发租约持久保存。
- `store.ts` 处理幂等、资源所有者、版本、领取租约、fencing、取消竞争与原子提交；`worker.ts` 处理后台执行、心跳、自动恢复和安全日志。
- `tools.ts` 的固定目录调用真实 DeepSeek/知乎；`flows.ts` 实现路线、学习搜索/直答/回答、问博主和博主搜索。R4 只写阶段计划，`staged-plan.ts` 编译 ID/边；`card-tools.ts` 将 C 编号限定在本次选卡范围，每操作一段，逐段检查证据片段属于对应材料版本；首次在同一 L-answer 内先逐卡审阅贡献，再综合生成正文。新合同检查点为 `L-answer:cards-v2@goal-v1`，旧批量挂卡结果不会直接复用。
- `context.ts` 保存调用副本的来源摘要，按配置窗口预检。不再截断 JSON 或把 reasoning 当正文。
- 路线发布前除共享文档校验，还通过与浏览器同一生成物的 `preflightLearningPath` 执行内容及物理拓扑编译。连续并列阶段共享的圆台横向道路按端点去重，各阶段的语义依赖完整保留；预检释放临时运行时句柄，不挂载 DOM 或 WebGL。
- `src/learning-v2` 移植已认可的 ux-ui 展示组件；服务端提供文章、图、对话，组件中不包含演示文章和计时生成答案。画布与文档共用一棵树；统一 MarkdownMath 处理公式，纯文本编辑与公式阅读分离。

- 首页品牌固定文字使用本地 SVG 字形；已答题是可展开的只读状态胶囊。3D 进度读取可取消、异步实例独立拥有 DOM，缓存内容未变不回写；显卡上下文丢失后可恢复场景，退出释放显卡上下文。正式网页允许模型贴图所需的本地 blob 读取，脚本仍仅同源。

- `materials.ts/materials-http.ts` 提供文件及收藏资料的持久导入。MD/Markdown/TXT 存正文；PDF 走知乎上传、异步任务、结果读取，保留解析正文与 API 总结，缺总结时由实际 LLM 全文分块压缩；规划优先完整解析正文，按当前目标压缩，正文缺失才使用标明为摘要的内容。所有本次路线资料进入每个概念，默认选卡包含资料；原始文件无知乎身份不入网。
- `zhihu-oauth.ts` 提供可配置的授权跳转、一次性浏览器绑定校验、服务端令牌交换、稳定用户 ID 回查、加密 Token 和账号 Cookie。`zhihu-data.ts` 对用户 API 强制使用该账号 OAuth Token。用户尚未申请凭证，实际 OAuth 尚未验收；最新裁决允许本地 `ZHIHU_OAUTH_MODE=mock` 体验完整授权及用户 API。演示使用独立账号/来源身份并显式标记，正式环境拒绝。

以上仅说明代码路径存在，完整接通和测试结果仍以第 3 节为准。

- 作者详情改为资料概览 → 单篇阅读 → 明确概念去向；学习足迹、主题推荐设置为次级入口。资料页脚的作者与足迹同一行对齐。无围栏程序块和公式表格竖线进入共享阅读预处理；缺式阅读与头像缓存分开，详见 [作者阅读修正](docs/author-reader-ux-2026-09-06.md)。

- 2026-09-07 展示修正：作者搜索复用共享 Composer/ChoiceMenu，单主题静态显示；轮播选择、拖动与松手归位分开。阅读使用共享版本与诊断，代码/公式边界和缺式原文提示覆盖全部学习视图；规范与样例见 [阅读渲染规范](docs/reading-rendering-standard.md)。

- 2026-09-07 作者输入与白底修正：请教目的、学习记录开关、问题与深度共用一个静态 Composer；补充背景使用不参与页面布局的 FloatingPanel。各页面的浅粉／浅紫交互底色改为白色，选中保留边框、勾选或文字反馈。文章与作者阅读共用 SourceComments，完整展示搜索接口返回的评论，不展示原文评论总数、覆盖说明或重复评论链接。数据与推荐设置入口已移除；作者轮播只预览一篇文章，点击或按 Enter 打开完整作者资料；详见 [最新视觉与评论验收](docs/visual-atlas-2026-09-07.md)。

- 首页品牌上方使用彩色山脉 SVG 简笔画和向上延伸的小径。共享大卡片封面按资源 ID 稳定选择 12 张刘看山生成图；3D 起点、载体、概念和终点标识按结构角色分配，不再限定示例 ID。知识卡用对应的罗盘、书本和想法标识，白色卡面及斜向轻阴影；上游 renderer 与宿主的分工见 [视觉资产记录](docs/visual-atlas-2026-09-07.md)。

## 2. 当前产品目标

本轮 Agent 裁决：从用户最终想完成的成果倒推最小充分路线；论文、收藏、作品、求职和系统学习不能套同一目录。不同作者的建议按目的、基础、成立条件和取舍理解。访谈每题恰好三个建议加自定义输入，选项只帮助表达，原话具有同等效力；已知条件不重复追问。每个概念说明目的、足够的深度和检验任务；有资料时以真实片段说明直接或必要前置关联。最终目标贯穿后续所有学习与作者步骤，原话不可被摘要或作者观点替换。

本轮补充裁决：作者搜索输入框内统一承载请教目的与搜索设置，不使用动态边缘；补充背景为悬浮编辑，不展开输入框。菜单、选中态及阅读／知识工具面板统一白底，不用粉色填充表示选中；用户显式设置的卡片颜色与图例语义独立保留。移除数据与推荐设置入口。评论区仅完整列出 API 返回的非空评论，不展示总数、精选覆盖说明或查看全部评论链接；无返回评论则不显示评论区。作者预览卡固定只展示一篇代表文章，详情保留全部文章。路线及知识脉络封面使用刘看山生成素材；起点、载体、概念、终点的 SVG 标识覆盖示例与真实路线，视觉不更改学习状态。首页正上方绘制带颜色的山脉简笔画，呼应“循着脉络，登上高峰”；输入与工具面板保持白底。

1. 学习页两栏可调宽；研究与知识脉络共享聊天和深度。文章标题、完整 API 总结、作者/点赞/链接只展示真实返回字段，未知字段不补造。
2. 初次进入概念先出现概念名称的用户消息，三种问法三路搜索，收齐后筛选概念相关材料，再三路不同角度直答和最终整理。文章先可读，恢复失败不丢已有文章。
3. 知识根是概念，一级为文章及本条路线资料，其后为 AI/博主/自定义卡。正交树无限延展；每段一个依据、每卡一个父卡，不再前置/后置/并列或批注。回答工具每次操作只生成一段，并逐段回查引用材料中的证据片段；跨文章内容拆段，不将整篇默认挂在文章 1 下，也不强行轮换引用。路线 3D 可分叉汇合。
4. 选卡决定材料，默认全部文章与路线资料；打开资料详情默认选该文章。AI 快捷项与自由输入均在卡片后长卡；问博主找 1–3 位新作者，正常零位由刘看山补充。
5. 画布编辑跳到文档对应段落；原始文章与用户副本分开。支持增加同级/子级、配色、描边、分支删除、撤销和重做。
6. 新对话归档旧聊天，当前为空，文章、知识树、编辑与配色保留。切换研究/画布/文档不取消后台任务。
7. 路线仍为 R1/R-S/R2/R3/R3b/R4，最多三轮亲近访谈，每题三个建议加自定义输入；旧题保留但失效。答完当前题组自动生成阶段数组（如 A → (B,C) → D），程序编译关系后校验、原子发布，不建立知识；每个处理步骤保留明确状态名称。已答题默认收成同款状态条，可展开完整题目和答案，刷新后仍保留。 确认偏好、安排阶段、发布路线等答题后步骤显示在“已选择”下方，包括运行中和恢复历史时。 已答胶囊与前后状态统一行间距。
8. 请求先持久接收，刷新重连恢复同一任务，自动恢复只做未完成步骤。当前任务继续与用户重新制定是不同动作，后者新建路线。
9. 接收请求时固定深度和引用内容版本。新的深度只影响下一次操作；摘要、修复不得暗中改为快速。
10. 内部错误保持可诊断，但不在用户界面罗列 schema、Agent、JSON 等错误。已有内容可读，暂停可继续，失败不得伪造成功。

11. 首页艺术字随页面以确定的矢量字形呈现，不依赖外部字体、系统字体回退或网络加载时机。3D 退出不得留下迟到实例，渲染故障支持恢复当前路线，不重新生成学习内容。重新进入时在可交互前统一恢复角色、黄色光圈、导航起点与区域；不重播起点入场。最近一次“开始学习”的概念圆台用紫色标记，普通移动不覆盖该标记，也不改变学习完成。开始学习离开画布前保存该概念为返回位置。

- 作者详情的主流程围绕一篇明确资料与一个明确概念展开；默认展示该作者全部资料，主题偏好不能隐藏其他资料，也不能成为含义不清的全局权重。数学原始资料仍不可覆盖，缺式补充必须与作者原文区分。

## 3. 验收账本

2026-09-07 目标访谈重构：524 项测试、类型检查与构建通过；五类合成目标使用真实 provider 生成路线，论文和 3D 样本走通首次学习，并对实际来源再验讲解收敛。自定义答案、只读历史及自动发布经独立浏览器操作验证；未知个人画像有字面回查守卫。具体样本、接口限流恢复和剩余语义问题见 [本轮验收](qa/goal-agents-2026-09-07/README.md)。不据此声明模型事实全对或生产就绪。

- 2026-09-07 作者输入与白底交互：512 项测试、类型检查与构建通过；桌面与 390×700 实际窄屏视口验证浮层不改变输入框高度、字段保留、发送控件边界和白色选中态。图示文章原文总数 4、接口返回 1，已完整展开返回内容并提供原文入口；全量评论尚未由接口提供。详见 [本轮验收](docs/author-composer-white-surfaces-2026-09-07.md)。

- 2026-09-07 轮播/输入/阅读修正：508 项测试、类型检查与构建通过；构建仍有原有主包体积提示。浏览器完成双向拖动、快速 End/Home/左右键切换及停手采样；思考/主题菜单键盘选择、Escape、外部关闭、Shift+Enter 换行，以及 390px 窄屏发送按钮和菜单边界验收。截图对应穹隅之末来源的四段推导代码原样显示且无数学误渲染；Dao1023 缺式来源的独立 AI 讲解实际显示 134 个公式，未解析和公式内部纵向滚条均为零，原摘要可展开。30 个异常/正常边界样例可通过 QA 页面复验，320px 卡片包括超宽矩阵无内容溢出。实际观测与离线回归不等于任意来源均可完整恢复或全设备性能保证。

- 2026-09-06 连续并列阶段：现场路线在第二组并列圆台重复生成物理连线而无法初始化；旧回归仅覆盖文档校验，未覆盖实际运行时。新增用同一 vendor 编译器执行的 259 个形状/边界样本（256 种四阶段宽度组合，加故障形状、16 阶段及 24 载体；故障形状覆盖 64 概念和单载体 12 概念上限），旧包稳定失败，新包通过。主项目 471 项测试、check/build 通过；上游功能与模块边界通过，独立演示包仍比 340 KiB 门槛超出 2.7 KiB，未放宽。原路线打开、跨并列阶段移动、退出重进、整页刷新及恢复后再次移动按浏览器实际验收记录，不等于全部设备或生产上线保证。
- 改造前：340 项旧测试通过，TypeScript 11 项错误。这不是集成验收。
- 本轮全量回归：`npm test` 390/390，`npm run check` 与构建通过；旧模块替身/规则测试仍计入总数，不能把总数都当作新集成测试。构建仍提示主 bundle 偏大。
- 持久任务与新工具测试：幂等、账号隔离、库重开、取消竞争、流截断、原子回滚、单父树、并行检查点、同 Agent 修复、上下文全量分块/缓存、编辑三方合并、provider 池、JWT、路线续跑、短引用卡片工具。阶段编译穷举了四阶段每阶段 1–4 载体的 256 种组合。
- 公式回归：常见定界符、漏符号、转义、矩阵/对齐环境、缺闭合括号、代码/链接/金额保护、宏限制；同时检查实际 Markdown AST，避免检测到公式却在页面吞掉后文。
- 真实 provider：路线从问题到发布、三路搜索/直答到首次学习、AI 追问、1–3 位新作者文章卡、普通回复及真实摘要调用均有本地执行证据。一次输出截断和一次搜索临时故障由同一持久任务自动恢复；这证明恢复有效，不证明上游没有故障。
- 浏览器：选择题刷新/确认保留、8 条明确路线步骤、文章/聊天/画布/文档公式、双击定位编辑、立即切换保存换行、展开按钮在卡内、引用划选、快捷 AI 在唯一父卡后长卡通过。本轮一次真实 AI 卡片回答约 2.34 秒，属于样本而非 p95。
- Docker：最终 API/网页镜像构建成功；本机 Caddy HTTPS + 正式构建网页 + PostgreSQL 验证安全 Cookie、真实路线、历史恢复、两页 PDF 和中文附件。发现并移除参考样式的第三方字体请求后，浏览器控制台无错误。
- 补检：修复 3D 缓存引用比较导致的无限更新、退出后的迟到挂载、嵌套 React root 卸载竞争及旧路线重复文档 ID 的位置串用风险。395 项测试通过；浏览器 8 次延迟请求快速导航、缩放、显卡丢失后场景重建、18 次完整进出及刷新恢复无控制台错误/警告。已答状态条键盘展开/刷新和学习核心交互复验通过；正式 HTTPS 镜像复验矢量品牌、3 条已答胶囊和 3D 贴图无控制台错误，见工程记录。
- PostgreSQL：独立数据库 gate、两个 Node 进程分领 6 个任务且不重复提交、API/数据库实际重启后内容恢复、备份恢复到新库并核对资源/完成事件。未做正式用户规模的压测，也未连接用户正式身份网关。

- 首答引用补检：真实资源发现 6 段共用一个 `after:C1`。改为扁平逐段操作、材料依据回查和首次逐卡审阅后，本次 18 篇材料生成 17 段、引用 10 篇；指定旧首答已在版本守卫下原子修复，浏览器引用与父卡一致并可刷新恢复。单篇、多篇选卡真实调用通过，401 项测试、类型检查与构建通过；见工程记录的引用修复条目。

- 资料与账号接入补检：全量测试 423/423、产品不变量 11/11、构建通过。真实 PDF API 解析与 PDF/Markdown 联合路线生成通过；7 个概念的路线进入首次概念后保留 2 份上传资料，并收集 19 篇知乎内容、生成 8 段单父卡，新对话保留树。浏览器检查行内矩阵、资料预览/复用/刷新恢复及 390×700 视口；知乎 OAuth 和收藏读取仅以接口替身验证，待申请凭证和官方用户信息合同后实测。详细资源与边界见 [资料与知乎账号接入](docs/materials-zhihu-integration-2026-09-06.md)。

- 搜索范围与公式补检：全量测试 467/467、类型检查及构建通过。实际全网搜索一次返回 20 条（18 条 CSDN），只有知乎站内证据进入作者管线；真实博主结果三张头像和 Coverflow 交互通过浏览器检查。本地演示收藏与上传文件经实际模型制定路线并进入概念，保留 5 份资料；修订提示词后的实际追问渲染 20 个公式并新增唯一父卡下的内容卡。用户截图中一条原始 API 摘要已缺公式内容，界面明确提供原文入口，不能用渲染修复声称还原。真实 OAuth 仍待凭证，详见 [本轮实现与验收](docs/search-scope-formulas-coverflow-2026-09-06.md)。

- 作者阅读与足迹修正：全量 478/478、类型检查、构建通过。真实浏览截图中的缺式来源与 NumPy 来源，补充公式和三段代码正常呈现；两种视口下 20 张资料卡左右中心线对齐。明确资料导入的幂等行为、跨主题多文章和单篇导入边界有回归，详见 [本轮研究与验收](docs/author-reader-ux-2026-09-06.md)。

完整范围与证据见 [工程记录](docs/backend-rebuild-2026-09-06.md)。这里只记录本次验收，不能据此承诺任意损坏公式或任意外部服务调用 100% 成功。

- 3D 本次再现并修复“恢复后下一次移动”起点冲突：恢复提前到 actor 初始化之前，导航从实际位置建立；开始学习前记住概念锚点，紫色最近学习标记与普通移动独立。功能回归与主项目 401 项测试通过；上游演示包体积仍超限 2.6 KiB，见工程记录。

## 4. 工程决策与剩余上线工作

### 4.1 已按本次授权选定的工程方案

存储、事务、任务去重/恢复/取消、事件序号和深度冻结点采用 [工程记录](docs/backend-rebuild-2026-09-06.md) 的方案；它们是本次实现选择，不冒称此前已经由用户逐项裁决。MCP 不作为每个 LLM 的强制包装。

2026-09-06 本轮新增裁决：普通博主搜索入口空白，历史显式恢复；作者 API 身份绑定留在服务端，不向用户展示“身份待确认”。首页资料支持 pdf/md/markdown/txt 和用户明确选中的知乎收藏夹/公开创作。全部本次资料随路线进入各概念的资料列表，按当前概念相关性用于讲解；R4 的 attachmentRefs 表示内容关联，不再控制资料是否可见。文件无知乎作者时不得入作者网络。收藏表示兴趣，不能冒充“有帮助”或主动使用加分。实现与外部阻塞记录见 [资料与知乎账号接入](docs/materials-zhihu-integration-2026-09-06.md)。

2026-09-06 后续裁决（本轮实施）：首页上传图标左侧增加资料范围菜单，上部“全知乎 / 全网”，分隔线下“仅知乎收藏夹”及收藏夹名称。附件在 Composer 内顶部紧凑显示。范围随路线冻结并继承至概念学习：全知乎沿用站内搜索；全网每路同时涵盖知乎站内和全网 API，两者独立检查点，同组顺序调度控制并发；仅收藏夹不触发外部检索，以明确选中的收藏及上传文件为依据，三角度整理用模型读取资料。显式问博主仍使用知乎检索。非知乎搜索证据没有作者网络身份。OAuth 和授权内容域先采用明确标示的本地 mock，与正式接口同合同，生产禁止开启；其他模型、搜索及 PDF 服务仍使用真实 provider。博主结果采用 Coverflow 卡片交互，网络图样式保持。公式图片不能静默隐藏，有明确 LaTeX 时转为公式，否则保留安全的来源图片。

本轮继续修正：旧知乎资料按准确原文 URL 回查头像并持久缓存，不能按作者名猜测。来源缺式时提供标识为 AI 整理的独立阅读版，原始摘要仍可展开，知识卡、引用和权重不变。初始作者目录也使用官方 Coverflow，详情在当前卡片下方显示。示例知识脉络和示例路线进入学习也复用 LearningWorkspace 的画布、文档、研究和对话组件；示例编辑仅保存到当前浏览器标签页，明确标示示例，不提交真实生成任务。作者卡片展示真实昵称、头像、认证图标与认证文案；没有认证时只显示中性的来源描述。详情采用统一字号和固定响应式面板宽度；卡片镜像反射改为右下 45° 投影。

### 4.2 仍需实际验收或外部配置

- 生产 DATABASE_URL、已验证模型窗口和公开源；身份可接可信网关或本轮知乎 OAuth。用户已明确尚未申请知乎凭证，先完成可配置接入。还需补齐官方用户信息接口合同和验证 state 回传；本地隔离会话不能宣称知乎登录完成。
- 首页 PDF/MD/Markdown/TXT 已接资料管线；PDF API 文字文档实测通过，扫描件和复杂图表仍需扩充验收。正式内容保留/删除期限、账号删除与备份保留政策仍未确定。
- 大规模不可压缩结构分区、语义检索质量、长对话压缩质量和正式负载/恢复演练。
- 3D 位置以 renderer 不透明场景状态按账号/路线资源 ID 保存到服务端，不用位置推导完成进度；同浏览器移动后重开已核对角色所在载体；跨设备的完整镜头视觉验收仍需补齐。
- 没有官方稳定 authorId 的 API 条目当前使用证据约束的身份后备标识，不按名字合并，收藏返回稳定作者标识时可聚合其多篇文章，相同原文链接可补齐搜索证据的作者绑定；其余跨文章作者身份完备性仍是缺口；这类记录不能因为同名而擅自认定是一个人。

### 4.3 博主搜索与网络重构（本次已授权）

用户已认可[完整设计](docs/author-network-design-2026-09-06.md)，补充付费咨询与邀请回答的找人目标，并要求重构 UI、保留网络图谱样式。第 2 节增加：文章作者自动发现；显式追问形成主题使用足迹；有帮助/少推荐可撤销；network-first 后仍探索新作者；证据解释适配、准备可编辑请教简报、回到来源卡片。新合同见 agent-specs.md 第五节。已落地的代码及本次验证如下；不能把旧 high/low 当作用户认可或联系记录。

- 后端默认入口使用 `authors-search.ts`、`authors-network.ts`、`authors-http.ts`：先读网络、两路新搜索、模型选择材料/段落编号、程序回查依据并按人去重。N2 同一作者可保留多篇相关材料，最终列表最多三位。
- 作者索引保留多文章、多主题、当前及归档对话中的实际问题和仍存在的卡片；每次搜索的发现关系单独保存，重复请求不会覆盖旧问题。使用积分按主题计算，同一命令跨作者分摊，原始关系不受偏好清除影响。
- UI 查阅 MentorCruise、ADPList、GrowthMentor 后，改为复用知识脉络/路线规划的 PeakHero、PeakTabs 和页面样式。搜索表单、人物目录、结果、详情抽屉、主题筛选和可编辑请教简报已实现；网络原有 SVG 点线/配色/拖动缩放保留。
- 本次真实验证：搜索显示三位人选；同一候选集开启/关闭积累时顺序确实发生变化，测试反馈已撤销；不相关材料被拒绝，相关材料成功加入矩阵学习并在后续对话生成一张唯一父卡。作者详情随之显示 1 次主动使用、搜索与学习两个主题及实际问题/卡片回链。
- 本次全量测试 411 项通过（其中新增作者模块 10 项），类型检查、构建及 11 项产品不变量检查通过；构建仍有主包体积提示。多篇文章/多概念/归档问题/搜索不覆盖、角色去重、反向定位、账号隔离、幂等冻结、反馈撤销和事务回滚均有可执行覆盖。测试总数含旧模块测试，不等于生产全链路或负载保证。
- 本次尚未重建 Docker 镜像或重新做 PostgreSQL 多进程 gate；生产身份、负载、作者稳定身份字段的覆盖率、匹配/压缩语义质量仍需继续验证。没有稳定 ID 的同名作者不跨文章自动合并。

### 4.4 保留与迁移

旧数据库/浏览器数据不直接改成新来源树，不伪造文章来源。旧记录可以只读归档，新事实只由新的服务端资源写入。根 AGENTS 与 Cursor 规则须引用本版合同。
