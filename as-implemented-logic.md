# ThreadPeak 现状对照清单

> Agent 上下文 / 压缩 / 提示词 / 输出：[`agent-specs.md`](./agent-specs.md)。  
> 身份：现状与目标的对照清单，不是完成证明。  
> 分层：第 1 节记录**当前代码实际会做什么**；第 2 节记录**已裁决的重构目标**；第 3 节只记录二者差异；第 4 节记录已消歧口径与仍待补合同。两类内容不得互相冒充。  
> 落盘规则：对话里先陈述 → 你纠正 → 双方同意后才标「已共同确认」。未确认的内容标「待审」；语义确认但合同未齐的内容标「已共同确认 · 合同待补」。  
> `Qxx` 是原对话中的追踪锚点，允许不连续；每项实质裁决必须在本文自包含地复述，不能只靠 Q 编号。你判定为错误的实现统一进入第 3 节「对照问题账本」。

| 项 | 已裁决 |
| --- | --- |
| 粒度 | 每页全部控件 + 跨页联动 + 后端模型/直达调用（提示词、输出约束、串/并联） |
| 结构 | 现状目录 → 目标深挖 → 对照问题账本 → 待补合同 |
| 文件 | 本仓库根目录 `as-implemented-logic.md` |
| 非产品壳层 | `path-lab` **已确认从产品清理掉** |
| 深挖顺序 | 目录确认后：先学习会话，再 Chat 路线制定，其余按走进产品的顺序 |

审阅状态：`待审` / `已共同确认` / `已共同确认 · 合同待补` / `你判定有问题`。实现成熟度不由这些状态代替。

---

## 1. 现状目录基线（页面范围已共同确认 · 控件细节仍可在深挖时补录）

- Q13：页面范围按走进产品的顺序齐；深挖发现遗漏控件时必须回补本目录，因此这里不把控件级覆盖称为已经完成。
- Q14：看起来不该存在的控件仍留在目录里，深挖到那一页再判「有问题」。

### 1.1 产品页面与控件

#### A. 授权落地页（未进入原型时）

| 控件 | 代码实际会做什么 | 状态 |
| --- | --- | --- |
| 夜间模式 | 切换亮/暗，写入本机主题 | 待审 |
| 知乎授权登录 | 向本机服务要官方授权地址；成功则跳转知乎；缺配置或失败则页内报错，不假装登录成功 | 待审 |
| 进入本地原型 | 只把本机「已进入」开关打开，进入带侧栏的首页。不是服务端登录 | 待审 |
| 用户协议 / 隐私政策 | 已从授权页拿掉 | 待审 |

#### B. 壳层（进入原型后始终在）

| 控件 | 代码实际会做什么 | 状态 |
| --- | --- | --- |
| 收起/展开侧栏 | 只改侧栏宽窄。进入多数内页会自动收起 | 待审 |
| 问山字标 | 回搜索首页 | 待审 |
| 搜索 | 去首页。⌘/Ctrl+K 同样去首页并尝试聚焦输入框 | 待审 |
| 知识脉络 | 去知识列表 | 待审 |
| 路线规划 | 去路线列表 | 待审 |
| 博主网络 | 去博主页（默认落在「搜索博主」） | 待审 |
| 历史展开/收起 | 只开合历史列表 | 待审 |
| 历史条目 | 学习草稿：精确回到同一条路线+概念+那次对话。其它草稿：带回发送上下文打开 Chat。缺失草稿：侧栏报错，不跳转。列表标明「本地草稿」 | 待审 |
| 账号按钮 | 打开菜单。标题：本机原型账号，或「知乎已授权」（不编造姓名） | 待审 |
| 夜间模式（菜单） | 同授权页，改本机主题 | 待审 |
| 退出登录 | 清服务端会话 cookie（若有）+ 本机进入开关关掉 → 回到授权页 | 待审 |
| 设置页入口 | **壳层没有入口**。只能手改地址 `#settings` | 待审 |

#### C. 搜索首页 `#home`

| 控件 | 代码实际会做什么 | 状态 |
| --- | --- | --- |
| 快捷：路线制定 | 给输入框打上「路线制定」；再点一次取消 | 待审 |
| 快捷：图文模式 | 给输入框打上「图文模式」；再点一次取消 | 待审 |
| 输入框 / 发送 / Enter | 有字才发。写入本机会话草稿后打开 Chat。模式映射：路线→路线体验；图文→图文体验；其它（含问博主预填）→普通回答 | 待审 |
| 思考深度菜单 | 只改本组件显示，**不随请求发出**，也不和设置页同步 | 待审 |
| 资料范围 | 显式失败提示，不假装已选来源 | 待审 |
| 添加附件 | 显式失败提示，不假装已上传 | 待审 |
| 建议芯片（3 条循环文案） | 只填进输入框，不发送、不改模式 | 待审 |
| 推荐知识脉络卡片 | 打开该**示例**知识的概念列表 | 待审 |
| 推荐学习路线卡片 | 打开该**示例**路线的 3D | 待审 |

首页 Composer **没有**「快捷引用」按钮。

#### D. 问答 `#chat`

| 控件 | 代码实际会做什么 | 状态 |
| --- | --- | --- |
| 无发送上下文 | 整页失败，不预写问题、不用旧正文冒充已打开会话 | 待审 |
| 返回 | 回首页 | 待审 |
| 新对话 | 若已挂上一条我的路线：同路线新开一条路线对话。否则清发送上下文回首页 | 待审 |
| 普通回答区 | 先就绪检查，再流式：检索公开证据 → 模型写回答。失败明示。后续提问会**换成**这一问一答，不追加气泡；历史标题仍是第一次发送的问题 | 待审 |
| 图文回答区 | 始终失败，不出图 | 待审 |
| 路线澄清题选项 | 选中一题的一个选项（高亮） | 待审 |
| 下一题 / 生成完整路径 | 把已选答案交回生成流。最后一题后走剪枝和 3D 文档校验 | 待审 |
| 路线生成失败·重试 | 用原目标再跑一遍生成流 | 待审 |
| 进入学习路线 | 打开这条已发布「我的路线」的 3D。知识脉络此时仍未创建 | 待审 |
| 底部 Composer（非路线） | 追问。图文追问仍走图文失败；否则当普通回答。路线模式**隐藏** Composer | 待审 |

#### E. 路线列表 `#paths`

| 控件 | 代码实际会做什么 | 状态 |
| --- | --- | --- |
| 我的路线 / 示例路线 | 切换列表。我的路线只展示通过 3D 文档校验的；半成品不出现 | 待审 |
| 空态「去问山制定路线」 | 回首页 | 待审 |
| 路线卡片 | 打开该路线 3D | 待审 |

#### F. 3D 路线 `#path-3d`

| 控件 | 代码实际会做什么 | 状态 |
| --- | --- | --- |
| 返回 | 回到来源：Chat / 首页 / 路线列表 | 待审 |
| 无法打开·返回路线列表 | 去路线列表 | 待审 |
| 绿色载体 / 概念卡片 /「走到这」 | 由 3D 场景自己播动画和打开卡片。走进度只在这次打开里记得，刷新丢失 | 待审 |
| 学习/进入概念 | 进入该概念的学习会话。我的路线缺校验文档则整页失败，不拿示例路线顶替 | 待审 |

#### G. 学习会话 `#session-learning`（第一条深挖）

| 控件 | 代码实际会做什么 | 状态 |
| --- | --- | --- |
| 缺路线或概念 | 独立 404，不是「无法进入这次学习」，也不是回首页 | 待审 |
| 我的路线进入 | GET 已 settle 则复用，不重播生成。无则 L0a 三路并联知乎直答 → L0b 整理；首轮与确定性唯一根同一成功结果，「知识脉络」立即出现。跨重启仍只在进程内存 | 待审 |
| 示例路线进入 | 读标明示例的目录课文，不走首次回复接口 | 待审 |
| 返回 | 回到 3D 或知识概念列表；若 3D 文档已无效则去路线列表 | 待审 |
| 知识脉络 | 仅当本机已有该路线知识记录（或这次建图成功）才显示。打开该概念画布，并留下「回到对话」 | 待审 |
| 新对话 | 同一路线+概念新开空对话，写入本机历史。不重新生成首次回复 | 待审 |
| 首次回复正文 | 可划选。划选用户气泡无效 | 待审 |
| 划选工具条·添加到对话 | 原文变成 Composer 引用条，带上所在节点 | 待审 |
| 划选工具条·问博主 | 弹出提问框（需另写问题） | 待审 |
| 问博主提问框·提交 / ⌘Enter | 打开批注侧栏，请求问博主。有 1–2 位真实作者才当博主；零可信作者展示刘看山直达（不是博主卡）。成功只写入批注标记，不长结构节点。失败不装成功 | 待审 |
| 问博主提问框·取消 / 点外侧 / Esc | 关掉提问框，不发请求 | 待审 |
| 批注球 / 侧栏关闭 / 批注按钮 | 打开或隐藏批注侧栏；外链仅真实知乎 URL | 待审 |
| 图文模式按钮 | 只给 Composer 打标。发送后**失败**，不写图 | 待审 |
| Composer 发送 | 见 2.3 学习会话发送链 | 待审 |
| Composer 思考深度 | 只改本框显示，不随请求发出 | 待审 |
| 生成中状态 | 文案顺序：检索公开证据 → 判断在脉络中的位置 → 生成回答。这是阶段标签，不是独立 agent | 待审 |

#### H. 知识列表 `#knowledge`

| 控件 | 代码实际会做什么 | 状态 |
| --- | --- | --- |
| 我的 / 示例 | 切换列表。我的知识来自本机工作区，不是服务端列表 | 待审 |
| 空态「去看我的路线」 | 去路线列表 | 待审 |
| 知识卡片 | 打开该知识下的**概念列表**（还不是画布） | 待审 |

#### I. 知识概念列表 `#knowledge-detail`（未选概念）

| 控件 | 代码实际会做什么 | 状态 |
| --- | --- | --- |
| 返回 | 回知识列表或首页（看从哪进来） | 待审 |
| 空态「去看我的路线」 | 去路线列表 | 待审 |
| 概念卡片 | 打开该概念画布 | 待审 |

#### J. 知识画布 `#knowledge-detail`（已选概念）

| 控件 | 代码实际会做什么 | 状态 |
| --- | --- | --- |
| 我的路线画布门 | 必须同时读到已提交的图快照和同一份首次回复，根节点绑这份正文。对不上或缺失则失败，不发明根 | 待审 |
| 返回（画布顶栏） | **清掉概念，停在概念列表**。即使从学习会话进来也是这样 | 待审 |
| 回到对话 | 仅从学习会话进来才出现，回学习会话 | 待审 |
| 放大 / 缩小 / 重置视图 | 只改镜头，不改图 | 待审 |
| 重新排列 | 丢掉手动钉住的位置，重新排版 | 待审 |
| 拖动画布 / 拖卡片 | 平移或钉住卡片位置（本机视图） | 待审 |
| 点节点 | 当前代码会把点击的卡片记住，让下一次提问生成的新卡片挂到它下面 | 这不是目标功能；只有引用某张卡片里的内容时才挂到该卡片，不引用时挂到最近一次 LLM 回复卡，见账本 #29 |
| 边上的数字 | 打开「逻辑说明」浮层 | 待审 |
| 划选 / 添加到对话 / 问博主 | 与学习会话同类；问博主同样走真实接口 | 待审 |
| Composer 发送 | 普通提问走回答流并在**本机**长图。图文/问博主（无划选）失败且不写图 | 待审 |

#### K. 博主 `#authors`

| 控件 | 代码实际会做什么 | 状态 |
| --- | --- | --- |
| 搜索博主 / 博主网络 Tab | 只切换本页两块，互不写入 | 待审 |
| 搜索输入 / 发送 | 先查「当前用户博主网络」。**现场网络投影未接通 → 整次失败，不会因此去知乎凑人**。设计上：有网络命中最多 3 人即停；零命中才查知乎再由模型最多挑 3 人。结果是人，不是回答。不写入网络、不打开对话 | 待审 |
| 雷达上的放大镜（收起后） | 重新打开搜索框 | 待审 |
| 博主网络 Tab 内容 | 始终失败，不用示例星图或本机缓存冒充已提交网络 | 待审 |

#### L. 设置 `#settings`

| 控件 | 代码实际会做什么 | 状态 |
| --- | --- | --- |
| 个人信息 | 有 OAuth 则「知乎已授权」不编姓名；否则显式缺少身份 | 已确认保留 |
| 资料范围 | 显式失败（尚未实现） | 已确认保留入口、继续失败 |
| 清空本地历史 | 只清侧栏索引 | 已确认保留 |
| 退出登录 / 夜间模式 | 现也在账号菜单 | 已确认设置页也要有 |
| 默认思考深度 / 紧凑密度 / 减少动效 | 本页循环或开关，离开即丢 | **从设置页拿掉** |

#### M. Composer 跨页共用（出现处见上）

| 控件 | 代码实际会做什么 | 状态 |
| --- | --- | --- |
| 模式前缀 × | 清掉当前模式 | 待审 |
| 引用条 × | 去掉引用，学习会话会忘掉引用所属节点 | 待审 |
| 添加附件 | 只在首页出现；当前点击后显式失败，不产生已上传状态 | Q85：目标只允许首页上传 pdf / md / txt；学习页和知识画布没有附件按钮，见账本 #18 |
| Shift+Enter | 换行，不发送 | 待审 |

#### N. 目标 404（当前尚无独立页面）

| 入口 | 当前代码实际 | 重构目标 |
| --- | --- | --- |
| 未知地址/hash | 回到首页或落入局部失败页 | 用户直接访问未知地址时显示独立 404；正常产品入口在缺少合法路线/概念时不跳转，见 Q15 / 账本 #4 |

---

### 1.2 跨页联动链索引（逐条标明现状或目标）

1. **共同部分：授权成功 / 本地原型** → 壳层 + 首页。  
2. **现状：首页发送** → 本机草稿 + Chat。路线模式不建知识。  
3. **共同部分：Chat 路线生成成功且 3D 文档校验通过** → 「我的路线」出现在路线列表；知识列表仍空。  
4. **共同部分：进入学习路线** → 3D → 点概念学习 → 学习会话。  
5. **目标：我的路线第一次进概念** → 首次回复与确定性根节点作为同一个成功结果落成 → 「我的知识脉络」立即可用。  
6. **现状：学习会话划选加入对话 → 发送普通问题** → 检索 → 分类 → 生成 → 本机长图；这条将由第 2.3 节目标双轨替换。  
7. **目标：学习会话划选问博主** → 批注标记，不长结构节点；选中的真实作者可写入博主网络。  
8. **目标：学习会话「知识脉络」** → 画布右上角一律「回到对话」（最新对话）；左侧返回仍可回概念列表。  
9. **目标：侧栏历史** → 学习记录点哪条开哪条；Chat 记录重开整段对话。  
10. **目标：博主搜索** → Graph RAG 高权→低权→知乎；最多展示 3 位，知乎有效候选不足剩余名额时全部返回，不重复、不虚构。  
11. **共同部分：设置清空历史** → 侧栏变空；路线、知识和已固定首轮仍保留。

---

### 1.3 后端实际调用（待审）

现场是**一个**本机 HTTP 进程里的分阶段函数。没有 worker。界面「agent 进度」不是运行时。

`server/agent-runtime` 提供共用上下文组装、500k/300k 预算压缩、输出结构校验，以及真实 DeepSeek / 知乎端口。`server/path-generation` 已把产品路线制定接到 R1 → R-S 并联检索 → R2 → R3/R3b → R4 → 3D 校验发布；发布不写知识图。`server/first-learning` 已把我的路线第一次进概念接到 L0a 三路并联知乎直答 → L0b 整理 → 首轮与确定性唯一根同一 settle。`server/follow-up` 已把学习页/画布普通追问接到 G1 结构 LLM 与 G2 知乎直答并发；两路都成功才长新卡，G2 失败不长图，G1 失败仍展示回复。`server/authors` 已把划选问博主接到 A1 → A-S 并联检索 → A2；A2 正常零人才 A3 刘看山直答；真实作者按 authorId 写入进程内高权网络。博主搜索接到 N0 高权→低权，不足再 N1/N-S/N2；投影可用但为空可查知乎，投影不可用 fail-closed；知乎新作者写入低权。网络 Tab 列出已入网作者或空态。旧的 CandidateSet / `/api/paths/generate/stream`、普通回答冒充首轮、独立 GraphSurgeon bootstrap、串行检索+分类长图、live-service 单次检索+authorKeys 仍在源码中，标为 prototype。第 4.2 节持久化与异步空位未裁决。

#### 不是独立 agent（列出来以免对照时找幽灵）

| 名字 | 实际 |
| --- | --- |
| AgentStatus | 前端阶段文案 |
| 刘看山 | 普通回答的系统人格；问博主零作者时的直达回退。禁止变成博主身份 |
| GraphSurgeon | 旧独立建根函数仍在源码，标为 prototype。产品首次进入由 first-learning 在 L0b 成功时确定性建根 |
| 博主网络投影 | 进程内名单投影已接通（高权/低权记录）。关系图拓扑仍未裁决，页面只列名单 |
| 图文 / 会话正文 GET / 增量写图 API | 无对应服务端能力 |

#### 模型 / 直达调用（审查时你称为 agent 的现场实体）

**调用 1 · 路线候选生成**（DeepSeek，JSON 对象）

- 触发：Chat 路线制定流，在检索之后。主生成失败且仍有时间预算时，**再串行**调用一次（带驳回摘录）。  
- 系统提示要点：只输出完整 CandidateSet；载体 DAG；概念 1..N；问题数 0 或 2–3；证据 id 不得虚构；目标不清必须出题并把图缩成 2 载体 × 1 概念。  
- 输出约束：服务端再过 schema 和质量门（必须覆盖核心流程+验证，以及前提/练习/坑/深度之一）；再投影成 3D 文档，校验失败则整次失败。  
- 并联：同一时间窗口的 **5 条知乎检索**（路线/练习/坑/前提/深度）`一起发出`。近年不够才**再串行**扩窗检索。剪枝、投影与模型调用都是串行。

**调用 2 · 普通回答**（DeepSeek，自由文本，可流式）

- 触发：Chat 普通问答；学习会话/画布普通发送；**首次回复**走同一套阻塞回答（种子问法：请用普通概念讲解该标题）。  
- 系统提示全文：  
  `你是刘看山。只根据给定的公开知乎证据和知识脉络上下文回答。回答必须对准当前针对的节点卡片，不要虚构作者、链接或未出现的公式。证据不足就明确说不知道。不要输出密钥、内部 URL 或 HTML。单次回答不要超过必要篇幅。`  
- 用户侧会带：概念、针对节点、知识脉络摘要、引用、问题、最多 8 条证据。  
- 输出约束：长度上限；不是 JSON。检索失败当失败，不当空证据继续编。

**调用 3 · 脉络生长分类**（DeepSeek，JSON）

- 触发：学习会话/画布走**流式**回答且带了候选节点时，在检索之后、写回答之前，**串行**多一次。分类 JSON 解析失败会被**忽略**，回答仍继续。  
- 系统提示要点：只输出  
  `{"kind":"pred|succ|par","title":"≤13字","reason":"一句","mergeNodeId":null或候选id}`  
- 前端用这次（或启发式/用户口令）在**本机**长图。GraphSurgeon **不会**应用这次分类。

**调用 4 · 作者挑选**（DeepSeek，JSON）

- 触发：问博主在知乎检索并按作者归并之后；博主搜索若网络为空本应在知乎检索之后（现场因网络未接通到不了这一步）。  
- 系统提示要点：只从给定 `authorKey` 里最多挑 N 个（问博主 N=2，搜索 N=3）；禁止新增身份；零相关返回空数组；`{"authorKeys":[],"reasons":{}}`。  
- 服务端会丢掉不在候选里的 key。

**调用 5 · 知乎直达**（知乎 `zhida-fast-1p5`，自由文本）

- 触发：问博主在挑选结果为 0 之后。**没有**系统提示，只有用户消息：问题 + 公开证据标题链接。  
- 失败当 503，不当「没有作者」。

#### 管线怎么串（现场）

```text
路线制定：校验 → 规划 5 查询 →【并联】近年检索 →（不够则）扩窗检索 → 候选生成 →（可选）再生成 → 有题则停等选择 / 否则剪枝 → 投影校验 → 发布
普通回答（阻塞）：校验 → 知乎检索 → 刘看山回答
学习追问：G1 结构 LLM 与 G2 知乎直答同时开始；G2 成功即可展示回复；两路都成功才按 G1 结构 + G2 正文长新卡
普通回答（流式，首页/Chat prototype）：检索 →（可选）生长分类 → 流式回答
首次回复：按 路线::概念 查表，有则永不重生成；无则 L0a 三路并联知乎直答 → L0b 整理成 content，并与唯一根同一 settle
建图：不再作为独立产品阶段。L0b 成功时确定性创建一图一根；POST /api/learning/graph 在 first-learning 接通时只复用或 409，不单独 bootstrap
问博主：校验必须有问题+划选 → A1 拆 2–3 问 →【并联】A-S 知乎搜索 → A2 只从输入 ID 选 1–2 人；selected 则回查原候选展示并写高权；no_suitable_author 才 A3 刘看山第一人称直答。A2 provider/结构失败不走 A3。结果只做批注
博主搜索：N0 高权 → 低权；不足 3 或网络为空 → N1 拆问 →【并联】N-S → N2 补 remainingSlots；投影不可用 fail-closed；新作者写低权；最多 3 人，不足则全部返回
OAuth：授权地址 → 回调换 token → 进程内会话。与上述生成管线无关
```

持久化：路线会话、首次回复、图、OAuth 会话都在**进程内存**。重启即忘。所谓「首次回复永久复用」只在该进程活着时成立。

---

## 2. 逐条深挖

顺序：学习会话（进入 → 首次回复与建图 → 划选/引用 → 发送与长图 → 问博主 → 图文 → 进画布）→ Chat 路线制定 → 其余按走进产品的顺序。

### 2.1 学习会话 · 进入

| # | 主题 | 代码实际 | 你的裁决 | 状态 |
| --- | --- | --- | --- | --- |
| Q15 | 缺路线/缺概念/概念不在路线上 → 整页失败 | 学习页只读本标签页临时「当前路线+概念」；3D 无概念仍会跳进学习页并展示失败文案。未知 hash 现在会掉回首页，不是 404 | 用户端正常使用应不可能出现。3D/历史等入口没有合法概念就不跳转。用户乱改地址 → **直接 404**（不是「无法进入这次学习」说明页，也不是默默回首页） | 已共同确认 · 实现记入账本 #4 |
| Q16 | 示例第一课从哪来 | 三条示例路线各绑一张示例知识；每个概念有预写课文，根图由课文投影（`linear-map` 用了更完整的示例图）。进入示例概念不打首次回复接口、不走 GraphSurgeon | 示例是对外展示；示例路线与示例知识脉络一一对应且同步；首轮回复已经存在，进入概念不必再生成 | 已共同确认 |
| Q17 | 我的路线第一次进概念如何出首轮回复 | 种子句「请用普通概念讲解说明「标题」…」→ 串行知乎检索 → 「你是刘看山」写自由文本。再次进入仍会 POST 确保首答（内存命中则复用）。同概念若已有学习对话，会挂上最近那一条 | 第一次进入：三路**并联知乎直答**（具体讲解 / 是否争议 / 踩坑点）→ 再由一次 LLM 整理成首轮。再次进入：该概念上次聊天记录，不触发首轮。整理用的提示词你稍后给 | 已共同确认（提示词待补） · 实现记入账本 #1 |
| Q18 | 首轮与根节点、新对话 | 新对话清空追问气泡，第一课仍是那份正文；长图仍写同一张概念图。永久存储只在进程内存，重启会再生成 | 首轮回复就是知识脉络根节点卡片，永远固定。无论是否开新对话，触发过首轮后第一条永远确定。新对话只从根往外长新卡片，不换根 | 意图已确认 · 持久化记入账本 #2 |
| Q19 | 知识脉络按钮与根节点是否调模型 | 首答成功后再 POST 建图（无模型，只投影）。建图失败仍显示首答；按钮仅在建图成功或本机已有该路线知识时出现 | 首轮回复成功时，同一个成功状态里把概念名和首轮正文确定性落为唯一根节点；根节点不调 LLM，也不存在独立的「首轮成功但建根失败」产品分支。完成后立即显示按钮 | 已共同确认 · 实现记入账本 #27 |
| Q20 | 再次进入的等待 | 即使已复用也先显示「正在生成这次概念的首次回复」，等 HTTP | 已有首答时不得重播生成状态；可以有普通数据读取状态，但不能再显示「正在生成首次回复」。读取完成后恢复该概念最近一条对话 | 已共同确认 · 实现记入账本 #3 |

**Q15 已确认的避免规则：** 正常入口（3D 进入学习、历史重开、画布回对话）没有合法「这条路线上的这个概念」则不打开学习页。用户乱改地址/hash → 产品 **404**。不再把「无法进入这次学习」当作给用户看的状态。

### 2.2 学习会话 · 划选添加到对话

| # | 主题 | 代码实际 | 你的裁决 | 状态 |
| --- | --- | --- | --- | --- |
| Q21 | 哪些字能划、宿主是哪张卡 | 不能划自己的气泡。宿主=划选起点所在块，默认根 | 对话里不限制划哪里，自己之前的问题也可以划。划选**助手回复**→ 宿主就是那张回复卡。划选**自己的问题**→ 宿主是**该问题对应的那条回复卡** | 已共同确认 · 实现记入账本 #5 |
| Q22 | 添加到对话是否立刻发送 | 只变成输入框引用条，不发送 | 只进引用条，不自动发送 | 已共同确认 |
| Q23 | 空问题 / 无引用能否发 | 有引用或有字就能发；无引用则不带原文 | 必须写下问题才能发。可以不点「添加到对话」：此时默认引用 **LLM 最新回复的完整内容**，宿主见 Q25 | 已共同确认 · 实现记入账本 #6 |
| Q25 | 无引用时宿主是哪张卡 | 无引用则宿主常落到根或上次划选残留 | 没点「添加到对话」→ 宿主=**最近一次 LLM 回复对应的卡片** | 已共同确认 |
| Q26 | 失败回复能否划选、图会不会动 | 失败助手气泡仍可划；失败问答也可能被长图路径碰到 | 回复失败则图谱不变。失败的那一轮对话划选**不出工具条**（划自己的失败问题与划失败的 LLM 回复同一套逻辑） | 已共同确认 · 实现记入账本 #8 |
| Q24 | 一次划过两段 | 仍出工具条，宿主只认起点那块 | 一次划选不得跨两张卡片；跨了不出工具条 | 已共同确认 · 实现记入账本 #7 |

### 2.3 学习会话 · 发送后对话与知识脉络（意图已确认 · 提示词待补）

**你要的（意图，提示词未写）：**

发送之后 **对话界面** 和 **知识脉络** **并发**，不是先答完再长图。G1 与 G2 的提示词共享同一份「宿主上下文 + 用户问题 + 大致内容方向」约束，使结构判断和回答围绕同一方向；不增加第二次语义复核调用。

**知识脉络这一路**

1. 上下文是一份 **JSON 图谱结构**，范围是问题所挂宿主卡及其：
   - 该卡全部内容
   - 并列节点
   - 所有前置节点、后置节点
   - 节点之间的链接逻辑
2. 问博主批注若出现在这些段落里，也要进上下文，并标明这是批注。
3. 这段 JSON 达到或预计超过 **500k tokens（500,000，即 50 万）**时：压缩各卡里的 LLM 回复正文，**不得打乱 JSON 结构**。500k 是包含系统提示、结构包装和预留输出在内的整次调用总预算；不能等请求已经超限后再压缩。
4. **一次 LLM** 读「这份图谱 JSON + 用户问题」，输出三件事：
   - 新节点相对宿主是前置 / 后置 / 并列（按用户理解的因果：看不懂某概念要解释→前置；要例子→并列；下一步怎么做→后置）
   - 节点标题（由问题总结）
   - 边的逻辑说明：第一人称、把问题收成顺畅讲解，例如「这个概念理解之后，接下来就可以看……」用来接上两张卡
5. 根节点仍是首轮回复，本路 **不** 为根再调模型。

**对话这一路（并发）**

1. 上下文只指**当前这一次对话的全文**（含问博主批注，并说明那是批注），不包含该概念的其他历史对话。
2. 当前对话全文达到或预计超过 **500k tokens** 则总结，始终压在该次调用总预算内。
3. 回复调用 **知乎直答**。强调：**只有首轮**才是三路并联直答再整理；**追问不单独检索**。

**两路汇合（Q27 已确认）：**

- 聊天可以立刻走直答（可流式）。
- **两路都成功才往图上落新卡**（结构用结构 LLM，正文用直答）。
- 直答失败：丢掉结构结果，图不动。
- 结构失败、直答成功：对话仍展示回复，不长图。

**批注（Q30 已确认）：两路上下文都带**，并标明「问博主批注」（作者卡与刘看山直达分开标）。

**代码实际：**

1. **串行**：知乎检索 → 另一次「结构编辑」LLM（JSON：`kind/title/reason/mergeNodeId`）→ 再流式「你是刘看山」写回答。分类解析失败就忽略，回答照旧。
2. 图谱上下文是 **全文纯文本**，默认塞 **整张图** 所有节点，阈值约 **22 万字**；压缩时也不是「邻域 JSON、只压回复」。
3. 回答成功后才在 **本机** 长图。GraphSurgeon 不管增量。
4. 追问不是知乎直答。批注不会作为标明身份的字段打进图谱 JSON。
5. 界面阶段文案「检索 / 判断位置 / 生成」是串行进度，不是两路并发。

### 2.4 学习会话 · 划选问博主（意图已确认 · 拆问/判定提示词待补）

问博主**不是**和发送双轨里的结构 LLM 并行。图谱侧**不调 LLM**。

**入口（Q32）：** 必须先划选，工具条上才出现「问博主」。没有划选就不能问博主。

**编排（已按你后一轮更正）：**

```text
等价拆问 2–3 个类似问题
    →【并联】知乎搜索
    → 把全部「博主 + API 返回的文章总结 + 文章链接」交给 LLM
    → LLM 判断哪些总结能回答用户问题，挑 1–2 个
        ├ 找到：返回 JSON
        │   { 用户问题(LLM整理版), authors: [{ authorId, 博主姓名, evidenceId, 文章总结, 文章链接 }] }
        │   展示（算法拼接，不再调 LLM）：
        │     姓名、总结，然后「详细内容可以阅读我的文章 {链接}」
        │   同时把 authorId、载体层、概念层、LLM整理的问题、博主姓名
        │     做成嵌入向量写入博主网络（之后用 Graph RAG 搜）
        └ 没找到：返回英文「没找到合适的博主」
            → 立刻知乎直达，刘看山第一人称
```

图谱侧仍无结构 LLM：成功后只同步批注标记；点击才在节点右侧出同等大小展示卡；点卡外消失。

拆问 / 判定的提示词仍待你给。

**代码实际：**

1. 划选后工具条就有「问博主」（与「添加到对话」一起出现）。弹出提问框再写问题。没有划选不能发问博主。
2. A1 拆 2–3 个等价问法 → A-S 并联知乎搜索 → A2 只从输入 authorId/evidenceId 选 1–2 人。selected：按 ID 回查原候选，展示「姓名 + 总结 + 详细内容可以阅读我的文章 {链接}」，并写高权网络。no_suitable_author：A3 刘看山第一人称知乎直答，不入网。A1/A-S/A2 provider 或结构失败不走 A3。
3. 成功后只打开批注侧栏，不长结构节点。
4. 普通输入框关键词「博主/作者」不会绕过划选要求。

### 2.5 学习会话 · 图文 / 新对话 / 进画布

| # | 主题 | 代码实际 | 你的裁决 | 状态 |
| --- | --- | --- | --- | --- |
| Q39 | 图文模式 | 按钮或文案触发后固定失败，不写图 | 产品尚未接通：必须失败，不得用假图冒充。编排另开专节 | 已共同确认 |
| Q40 | 新对话后根上的批注 | 批注按对话 id 分份，新对话侧栏空；图共享但问博主被做成并列卡 | 首轮根永远固定，**首次回复正文上的批注标记跨新对话保留，仍能看见** | 已共同确认 · 实现记入账本 #28 |
| Q41 | 画布右上角「回到对话」 | 仅从会话进来才出现；侧栏进画布没有。左侧返回去概念列表 | 无论从会话还是侧栏进画布，右上角都是「回到对话」，回到该概念**最新一条对话** | 已共同确认 · 已实现 |
| Q42 | 画布提问/划选/问博主 | 画布自己发、自己长图，不写回会话那条对话 | 与**最新对话**同一套规则，并**实时同步**到这份记录；回到对话立刻看到（含正在输出的字）。不是分屏双开 | 已共同确认 · 实现记入账本 #12 |
| Q43 | 后续卡上的批注 | 同 Q40 现场 | 概念图上**所有**批注标记都保留，不单是根上的 | 已共同确认 · 实现记入账本 #28 |
| Q44 | 示例侧栏进画布、还没有对话 | 示例可直接进画布；回到对话可能对空 | 示例路线、首轮、根节点都是预置设计，不现场生成。「回到对话」在还没有用户对话时 = **新建对话**（根仍是那份预置首轮） | 已共同确认 |
| Q45 | 实时同步粒度 | 两页不能同时铺开 | 同一份最新对话；画布写入这份记录；回到对话零等待看到。左侧返回仍可回概念列表 | 已共同确认 |
| Q46 | 示例已有对话时回到对话 | 侧栏进画布没有该按钮 | 已有则回该概念最新对话；没有才新建 | 已共同确认 |

学习会话的已裁决语义深挖到此；提示词、持久化与异步共同状态仍列在 2.12 / 4.2，不能视为实施合同已经完整。下一块：Chat 路线制定。

### 2.6 Chat · 路线制定（意图已确认 · 提示词待补）

载体层 ≈ 学科 / 系统课程 / 书籍层。下面第一份结构只是 R2 用来汇总所有可能载体、概念与争议的**探索 JSON**，不是最终路线 JSON。探索 JSON 的动态载体名、概念名使用中文（Q48 更正）。

探索 JSON（LLM-B）：

```text
{ 载体层1: { 概念层1: { 争议: true }, 概念层2: { 争议: false }, ... }, 载体层2: { ... }, ... }
```

最终路线 JSON（LLM-D）必须使用固定字段、稳定 ID 和显式边，不能用载体名或概念名充当对象键。每个概念有一段 **详细描述**（给该概念首次学习用：讲解应偏向什么、为了解决什么）。若路线生成时带了附件，只把与该概念相关的信息写进详细描述，并记录相应附件来源 ID；学习页不再带原件。

```json
{
  "version": "1.0",
  "routeId": "route-uuid",
  "title": "路线标题",
  "carriers": [
    { "id": "carrier-uuid", "title": "载体名称", "description": "载体说明" }
  ],
  "concepts": [
    {
      "id": "concept-uuid",
      "carrierId": "carrier-uuid",
      "title": "概念名称",
      "hasDispute": true,
      "detailedDescription": "讲解偏向与要解决的问题",
      "attachmentSourceIds": []
    }
  ],
  "carrierEdges": [
    { "id": "carrier-edge-uuid", "fromCarrierId": "carrier-uuid", "toCarrierId": "carrier-uuid", "reason": "推荐流转关系" }
  ],
  "conceptEdges": [
    { "id": "concept-edge-uuid", "fromConceptId": "concept-uuid", "toConceptId": "concept-uuid", "reason": "推荐学习关系" }
  ],
  "entryConceptIds": ["concept-uuid"],
  "terminalConceptIds": ["concept-uuid"]
}
```

数组顺序只用于稳定展示，真正的分叉与汇合由显式边表达：一个节点多条出边表示分叉，多条入边表示汇合。边表达推荐的内容流转，不是解锁条件；3D 中所有节点都可进入。

**你要的编排：**

```text
用户输入目标
    → LLM-A 拆问：把目标拆成多种知乎问法
       必须覆盖「如何学 / 入门 / 理解」等正常学法（不同博主思路会不同）
       以及「常见坑 / 争议 / 容易误导的学法」
       路数 4–5，**最多 5**（并发上限）
    →【并联】按这些问法检索知乎
    → LLM-B：根据全部检索，整理所有可能的载体层、概念层、是否争议（JSON；看不出争议也要自行分析，一般都有）
    → LLM-C：根据 LLM-B 输出生成 1–3 道小白选择题（JSON 模板；题数由模型判断；几乎总会有题），记为第 1 轮
    → 算法按序把题呈现给用户
    → 若用户没答完就追问：
         LLM 看追问 + 已有题
         ├ 无关 → 告诉用户先把题答完
         ├ 相关且未到第 3 轮 → 解释后输出新的 1–3 道题；旧题留在对话里变灰、不可再点
         └ 已到第 3 轮 → 可以解释，但不再换题，提示用户完成当前题组
    → 用户把当前这套题全部选完
    → LLM-D 路径生成（不再检索）：吃 LLM-B JSON + 选项 + 附件（若有）
       使用上面的稳定 ID + 显式边路线 JSON；载体可并行（一分二再合并）；每载体通常 2–3 个概念，偶尔 1 个
       每个概念层必须带一段 **详细描述**（讲解偏向、要解决什么；有附件则附件信息也写在这里）
    → 校验通过 → 「进入学习路线」按钮
    → 输入框一直在：此后普通发送 = 正常回复
       再次点「路线制定」模式 = 在同一对话框里再走一遍全流程
```

发布时仍不建知识脉络。

**Q52–Q59 已确认：** 拆问最多 5 路并联；选择题最多 3 轮（初始题组计第 1 轮），相关追问可在上限内换题，旧题变灰不可点；输入框一直在；生成后普通回复（与首页非路线发送同一套）；再开路线制定 = 全流程重跑并新增一条我的路线。「进入学习路线」去 3D，发布时不建知识。3D **不设锁**：显式边表达推荐流转和分叉/汇合，不控制节点能否进入。生成失败点重试 = 同一目标从 LLM-A 整段再来。

每套题必须带 `round = 1..3` 和 `active | superseded` 状态。旧题及已答选项保留在对话与 R4 输入中，但不能再次修改；若不同轮答案冲突，以较新轮次为准。只有当前 active 题组全部答完才进入 R4。

**代码实际：** Chat 路线制定走 R1 → R-S 并联 → R2 → R3/R3b ≤3 轮 → R4 校验发布；输入框一直在。失败从 R1 整段重试。发布不建知识。会话与 path-run 在进程内存。

### 2.7 路线列表 / 3D（意图已确认）

| # | 主题 | 代码实际 | 你的裁决 | 状态 |
| --- | --- | --- | --- | --- |
| Q60 | 我的列表是否展示半成品 | 只列出通过 3D 文档校验的 | 保留：校验没过的不出现 | 已共同确认 |
| Q61 | 空态「去问山制定路线」 | 只回搜索首页，不点上路线制定 | 回搜索首页并**自动选中路线制定**；不代写内容、不代发送 | 已共同确认 · 实现记入账本 #16 |
| Q62 | 3D 返回去哪 | 按来源回 Chat / 首页 / 列表 | **全部统一回路线列表** | 已共同确认 · 已实现 |
| Q63 | 走到这 vs 进入学习 | 走到这只播动画；进入学习才开会话 | 两套动作分开：走到这不创建会话、不建知识；进入学习必须带上概念否则不跳转 | 已共同确认 |
| Q64 | 3D 位置 | 走进度只在这次打开里，刷新/重进丢失 | **不锁进度，但每次进入 3D 要回到该路线上次的位置** | 已共同确认 · 实现记入账本 #15 |

**返回（已确认）：** 学习会话左上角 → **3D**；3D 返回 → **路线列表**。

**代码实际（其余）：** 点列表卡片进 3D。3D 返回统一回路线列表。学习会话返回 3D（打不开则回路线列表）。没带上概念进学习不跳转；乱改 hash 进独立 404。示例用标明示例的文档；我的缺校验文档则失败，不拿示例顶替。上次 3D 位置的数据形态仍未裁决。

### 2.8 搜索首页（意图已确认 · 芯片文案待补）

| # | 主题 | 代码实际 | 你的裁决 | 状态 |
| --- | --- | --- | --- | --- |
| Q65 | 首页快捷与问博主 | 只有路线制定 / 图文；无问博主快捷 | **不在首页加问博主** | 已共同确认 |
| Q66 | 流动建议栏（芯片） | 三句示例，点击只填字，不改模式、不发送 | 点击后**自动选中路线制定并把文案填入**。芯片要**全部改成适配路线制定**的文案。不代发送 | 已共同确认 · 实现记入账本 #17；新文案待你给 |
| Q67 | 推荐知识 / 推荐路线 | 标明示例，分别进示例概念列表、示例 3D | 保留，且一直能看出是示例，不和「我的」混 | 已共同确认 |
| Q68 | 资料范围、附件 | 点了显式失败，不限格式 | **附件允许上传，仅 pdf / md / txt**。资料范围继续失败（尚未实现） | 已共同确认 · 实现记入账本 #18 |
| Q69 / Q70 | 思考深度 | 现场有快速/智能/深度且不进请求 | **只有快速/深度两档。** 学习页开了深度 → 知识画布同样深度。首页开的深度不带到学习页 | 已共同确认 · 实现记入账本 #19 |
| Q71 | 资料范围 | 点了失败 | 继续失败，尚未实现 | 已共同确认 |
| Q72 / Q73 / Q85 | 附件进哪次调用与压缩 | 无上传；学习会话关附件 | **路线生成过程中每一次 LLM 都带着附件**（拆问/整理/出题/路径）。**开始学习、知识脉络输入框没有附件。** 首页普通 Chat 若带了附件，只跟着那次 Chat。整次调用总预算为 500k tokens：非附件部分 < 300k 时先压缩附件一次；非附件部分 >= 300k 时压缩非附件。详细边界见 `agent-specs.md` 0.1 | 已共同确认 · 实现记入账本 #18 |

### 2.9 侧栏历史 / 账号（意图已确认）

| # | 主题 | 代码实际 | 你的裁决 | 状态 |
| --- | --- | --- | --- | --- |
| Q74 | 点学习历史 | 精确回到那条学习对话（路线+概念+气泡） | **点哪条打开哪条**。画布「回到对话」才用最新 | 已共同确认 |
| Q75 | 点 Chat 历史（路线制定/普通问答） | 只带回原问题+模式打开 Chat；现场 Chat 几乎只有一问一答，追问会换掉问题 | **整段对话原样打开**（题、变灰旧题、生成结果、之后的普通回复）。记录丢了只侧栏报错，不跳空白 Chat | 已共同确认 · 实现记入账本 #21 |
| Q76 | 清空本地历史 | 只清侧栏名单 | **只清名单**，不删我的路线、知识图、已 settle 首轮 | 已共同确认 |
| Q77 | 设置入口 | 壳层没有，只能改 `#settings` | **账号菜单里加「设置」** | 已共同确认 · 实现记入账本 #20 |

### 2.10 授权页 / 设置页（意图已确认）

| # | 主题 | 代码实际 | 你的裁决 | 状态 |
| --- | --- | --- | --- | --- |
| Q78 | 授权 vs 进原型 | 授权跳官方或失败；进原型只开本机开关 | 两条入口都保留。缺配置不能假装登录；进原型不锁死整站 | 已共同确认 |
| Q79 | 用户协议 / 隐私政策 | 展示文字，点了没文档 | **不需要这两项**，授权页拿掉 | 已共同确认 · 实现记入账本 #22 |
| Q80 | 思考深度默认与范围 | 设置页循环且离开丢失；输入框另有一份不进模型 | **只有两档。** 默认永远快速回答。**学习页和知识画布同步**：学习页开了深度，进画布仍是深度。首页（或其它页）开的深度**不会**带到学习页。离开学习/画布这一对界面后回到快速 | 已共同确认 · 实现记入账本 #23 |
| Q81 | 设置页还留什么 | 身份、思考深度、资料范围、密度、减动效、清空历史 | **保留：退出登录、夜间模式、清空历史、用户身份、资料。拿掉：紧凑密度、减少动效、默认思考深度**（思考只在聊天输入框两档切） | 已共同确认 · 实现记入账本 #24 |

### 2.11 博主搜索 / 博主网络（意图已确认）

和会话里「划选问博主」不是同一条。本页搜的是**人**。

**你要的搜索编排（Q82–Q84 已确认）：**

博主网络里有两类节点（Graph RAG）：

- **高权：** 学习里问博主成功写入的（稳定 `authorId` + 载体层 + 概念层 + LLM 整理的问题 + 展示姓名）
- **低权：** 本页「搜索博主」从知乎捞到后写入的（稳定 `authorId` + 展示姓名 + 对应问题，没有载体层/概念层）

```text
目标最多展示 3 个人，按优先级补齐；有效候选不足时返回全部，不重复、不虚构：
Graph RAG 高权（问博主来源）
    → 不够 3：再补低权（搜索博主来源）
    → 还不够 3（或网络为空）：
         问题拆成 2–3 个类似问题
         →【并联】知乎搜索
         → 全部「博主 + API 文章总结 + 链接」给 LLM
         → LLM 从真实候选中补足剩余名额；若知乎有效候选少于剩余名额，就全部返回
         → 新从知乎来的人按 authorId 去重后写入网络为低权（authorId + 姓名 + 问题）
```

投影**未接通**仍整次失败，不去知乎（Q82）。接通但为空 → 跳过、走知乎。

**博主网络 Tab：** 有人就列出已入网博主（不要示例星图）；一个人没有就空态，不用「功能不可用」失败页。关系图画法未定，先名单。

**代码实际：** N0 先高权后低权；不足 3 或网络为空才 N1/N-S/N2。投影不可用则 503，不会落到知乎。投影可用但为空可查知乎。结果是人，不打开对话。知乎新作者写入低权。

**代码实际：** 网络 Tab 有人列名单、没人显示空态；不用示例星图，也不把旧载体→概念→问题图当成正式 topology。

### 2.12 尚未深挖完成的目标区域

以下内容不能再被「其余按现状」代替；在补齐前只算已发现的规格空位：

1. **普通 Chat：** 首页普通发送、路线发布后的普通发送都引用同一条目标管线，但它的上下文范围、附件输入、流式终态、失败/重试和历史落盘仍待补。
2. **图文模式：** 当前确认未接通时必须显式失败；未来真实图文编排尚未裁决，Q39 所说的独立专节仍待补。
3. **异步共同状态：** 路线、首轮、学习双轨、问博主、博主搜索都还需要请求身份、重复提交、取消、超时、部分失败、刷新恢复和迟到响应规则。
4. **附件上传生命周期：** 文件大小/数量、解析状态、移除、失败重试、重复文件与来源 ID 规则仍待补。
5. **OAuth 回调与 404：** 授权拒绝、state 失配、会话过期和独立 404 的可见状态仍待补。
6. **3D 位置：** 「记住上次位置」究竟保存角色坐标、镜头还是当前节点，以及何时写入/失效，仍待补。

---

## 3. 对照问题账本

| # | 来源 | 问题（你已判定） | 修正方向（你给出的） | 状态 |
| --- | --- | --- | --- | --- |
| 1 | Q17 | 我的路线首次进入误用「知乎检索 + 刘看山普通回答」串行出首轮 | 第一次进入：三路并联知乎直答（具体讲解 / 是否争议 / 踩坑点）→ 一次 LLM 整理成首轮。整理时必须带上路径 JSON 里该概念的 **详细描述**（讲解偏向、要解决什么；路线生成时若有附件，附件信息已写在这段里，学习阶段不再带原件）。再次进入不触发首轮，进该概念上次聊天记录 | 已实现（进程内；合同见 agent-specs L0a/L0b；跨重启见 #2 / 4.2） |
| 2 | Q18 | 首轮只存在服务端进程内存；重启后再进入会重新生成另一份正文，根节点不能保证永远固定 | 触发过的首轮永远是根节点正文；新对话不得替换它 | 待你补：存在哪、跨重启如何仍读到同一份 |
| 3 | Q20 / Q17 | 再次进入即使已有首答，仍闪「正在生成」并等待 POST | 已有首答则不重播生成状态；只允许普通读取状态，随后恢复该概念最近一条对话（含固定首轮根） | 已实现（进程内 GET 复用） |
| 4 | Q15 | 用户端会出现「无法进入这次学习」；乱改 hash 还会掉回首页 | 正常入口无合法路线+概念则不跳转。用户乱改地址 → 直接 404，不是学习失败说明，也不是回首页 | 已实现 |
| 5 | Q21 | 禁止划选自己的气泡；划选宿主只认起点块 | 对话内任意气泡可划。划自己的问题 → 宿主=该问题对应的回复卡 | 已实现 |
| 6 | Q23 / Q25 | 仅有引用、问题为空也能发；无引用则不带默认原文 | 必须有问题才能发。无引用时默认引用最新 LLM 回复全文，宿主=该回复卡 | 已实现 |
| 7 | Q24 | 跨两段回复仍出工具条 | 跨两张卡片不出工具条 | 已实现 |
| 8 | Q26 | 失败回复仍可划选 | 回复失败则图谱不变；该轮划选不出工具条 | 已实现 |
| 9 | 发送双轨 | 追问串行：检索→分类 LLM→刘看山；成功后本机长图；上下文不是邻域 JSON、阈值约 22 万字 | 发送后对话与脉络并发，两边提示词共享宿主、问题和大致内容方向，不增加二次语义复核。脉络邻域 JSON 与当前对话全文分别按 500k 总预算压缩。两路都成功才落卡；直答失败图不动；结构失败则只出聊天不长图 | 已实现（合同见 agent-specs G1/G2；问博主长图已从普通发送中拿掉） |
| 10 | 问博主 | 单次检索+挑选 LLM；成功另长并列节点 | 等价拆 2–3 问并联搜 → LLM 看总结和链接挑 1–2；输出必须绑定 `authorId` 与 `evidenceId`。找到后展示并按 authorId 写高权网络；找不到英文分支后立刻刘看山第一人称直达。图谱只同步批注标记 | 已实现（进程内；合同见 agent-specs A1/A2/A3；跨重启见 4.2） |
| 11 | Q41 / Q44 / Q46 | 仅会话进画布才有「回到对话」；侧栏进画布没有 | 侧栏进画布也是「回到对话」。有用户对话则回最新；没有（含示例预置根）则新建 | 已实现 |
| 12 | Q42 / Q45 | 画布提问不写回会话对话 | 画布与最新对话同一套发送/划选/问博主，写入同一份记录；回到对话立刻看到输出 | 已实现（同会话记录；进行中流式见 4.2 异步空位） |
| 13 | Q47–Q59 路线制定 | 写死 5 角检索 → 一次 CandidateSet（含题）→ 答完只剪枝；强制线性载体×每载体 1 概念；路线模式无输入框；追问会换掉唯一气泡 | 见 2.6 全流程。R2 探索 JSON 与 R4 最终路线 JSON 分离；最终路线用稳定 ID、节点数组和显式边。选择题最多 3 轮。3D 边只表达推荐流转，不锁节点。失败重试从 LLM-A 整段再来，再次制定新增一条我的路线 | 已实现（合同见 agent-specs R1–R4；跨重启见 4.2） |
| 14 | Q62 | 3D 返回按来源回 Chat/首页/列表 | 3D 返回全部统一回路线列表 | 已实现 |
| 15 | Q64 | 3D 位置刷新丢失 | 不锁学习顺序，但每条路线记住上次 3D 位置，再进仍停在那儿 | 待实现 |
| 16 | Q61 | 空态按钮只回首页 | 回首页并自动选中路线制定；不代写、不代发 | 已实现 |
| 17 | Q66 | 建议芯片只填字、文案不全是路线 | 点击自动选中路线制定并填入；芯片文案全部改为路线制定；不代发 | 行为已实现（选中路线制定并填入、不代发）；新文案仍见 4.2 |
| 18 | Q68 / Q72 / Q73 / Q85 | 附件点了失败、不限格式；学习/画布也有附件入口 | 仅 pdf/md/txt。路线生成各 LLM 带附件；首页普通 Chat 的附件只跟随该 Chat；学习页和知识画布无附件。总预算 500k tokens：非附件 <300k 时先压附件一次，非附件 >=300k 时压非附件。资料范围继续失败 | 已实现（大小/数量/删除生命周期仍见 4.2） |
| 19 | Q69 / Q70 / Q80 | 思考含智能且不进请求 | 只有快速/深度两档。默认快速。学习页与画布深度同步；首页深度不带进学习页 | 已实现（首页是否带入随后 Chat 仍见 4.2 冻结点） |
| 20 | Q77 | 壳层没有设置入口 | 账号菜单加「设置」 | 已实现 |
| 21 | Q75 | Chat 历史只带回一句问题和模式 | 重开整段对话；缺失则侧栏报错不跳空白 Chat | 已实现（进程内 path-run / 本地草稿；跨重启见 4.2） |
| 22 | Q79 | 授权页有不可点的协议/隐私 | 拿掉用户协议和隐私政策 | 已实现 |
| 23 | Q80 | 设置思考循环且离开丢失 | 只有两档。默认快速。学习页与知识画布深度同步；首页深度不带到学习页 | 已实现 |
| 24 | Q81 | 设置页含密度、动效、思考默认 | 设置页保留退出登录、夜间模式、清空历史、用户身份、资料；**去掉紧凑密度、减少动效、默认思考深度** | 已实现 |
| 25 | Q82–Q84 博主搜索/网络 | 网络不可用则 503；Tab 永远失败页；挑选最多 3 人 | Graph RAG 按高权→低权→知乎补到最多 3 人；知乎有效候选少于剩余名额时全部返回，不重复、不虚构。所有人以 `authorId` 去重，新来的写入低权。投影未接通仍失败；Tab 有人列名单、没人显示空态 | 已实现（进程内名单投影；拓扑仍见 4.2） |
| 26 | Q86 | 存在 path-lab 实验页 | **清理掉**，不进侧栏、不写我的路线 | 已实现（不进产品路由/侧栏/Vite 主入口；源码实验页仍在，不扩展） |
| 27 | Q19 | 首轮成功与建根被拆成两个可分别失败的页面阶段 | 首轮回复一旦成功，同一成功状态中确定性创建唯一根并立即开放知识脉络；根节点不另调 LLM | 已实现（进程内） |
| 28 | Q40 / Q43 | 批注按 conversation 分份，新对话后看不到旧批注 | 批注锚定概念图节点及原文位置；根和后续节点上的批注都跨新对话保留 | 已实现 |
| 29 | 宿主规则 | 现在只要点一下画布里的卡片，下一次提问生成的新卡片就可能挂到它下面 | 不能靠点击决定挂载位置。引用某张卡片的内容时挂到该卡片；没有引用时挂到最近一次成功 LLM 回复卡 | 已实现 |

---

## 4. 一致性审查（2026-09-02）

读法：第 1 节和第 1.3 节是**现场代码**；第 2 节是**你的裁决**。二者不一致是账本要改的实现，不是两套产品同时成立。下面只列「裁决和裁决打架」或「裁决没说清」。

### 4.1 已消歧口径（已拍板）

1. **附件**：附件只从首页上传。路线模式下，路线生成各 LLM 一直带着附件；路径输出只把相关附件信息写入对应概念的详细描述。首页普通 Chat 若带附件，只跟着该 Chat。开始学习和知识画布没有附件输入。
2. **token 预算**：写作 `500k tokens（500,000 / 50 万）` 与 `300k tokens（300,000 / 30 万）`。非附件 `<300k` 与 `>=300k` 是两个完整分支；学习双轨的两次调用分别按各自上下文计算 500k 总预算。
3. **深度思考**：学习页和知识画布同步（学习页开深度，进画布仍深度）。首页开的深度不会带到学习页。
4. **搜索博主人数**：目标最多 3 个。高权不够继续低权，再不够才查知乎；知乎有效候选不足剩余名额时全部返回，不重复、不虚构。
5. **路线结构**：R2 的中文动态键 JSON 只是探索结果；R4 最终路线必须是稳定 ID、节点数组和显式边。边表达推荐流转与分叉/汇合，不锁 3D 节点。
6. **学习双轨**：G1/G2 共享宿主、问题和大致内容方向提示约束，不新增二次语义复核。
7. **首轮与根**：首轮成功和确定性根落成是一个产品成功状态；根不调 LLM，不存在独立建根失败分支。
8. **宿主**：不存在点击节点选宿主。显式引用节点内容时挂该节点；不引用时挂最近一次成功 LLM 回复卡。
9. **路线追问题组**：最多 3 轮，初始题组计第 1 轮。
10. **对话全文**：学习追问只读取当前这一次对话全文，不读取该概念的其他历史对话。
11. **返回**：学习会话返回 3D；3D 返回路线列表。

### 4.2 没写死、实现时会猜

1. **问博主找不到**：英文「没找到合适的博主」是只作为程序分支值，还是短暂展示给用户？
2. **Chat 没发送上下文**：现场是失败页。Chat 空打开最终算 404，还是保留专用失败说明？
3. **输入框关键词**：问博主已要求先划选；普通输入框出现「博主/作者」时是否一律禁止自动切换模式？
4. **首页图文快捷**：未接通期间保留一个发送后必定失败的按钮，是否就是最终过渡体验？
5. **普通 Chat 目标管线**：上下文、附件、提示词、流式终态、失败重试和历史提交仍待补。
6. **图文目标管线**：真实图文编排仍待单独裁决；当前只有 fail-closed 过渡语义。
7. **首轮持久化**：唯一作用域、跨重启存储和并发首次进入规则仍待补。
8. **附件上传**：大小、数量、解析失败、删除和来源 ID 生命周期仍待补。
9. **异步共同状态**：取消、超时、部分失败、刷新恢复、重复提交和迟到响应仍待补。
10. **思考深度冻结点**：首页选择的深度是否带入随后打开的 Chat，以及运行途中切换是否影响已启动调用，仍待补。
11. **提示词与芯片文案**：明确待补，不能由实现者自行发挥。

### 4.3 已过时、容易误读（不是你改口打架）

- 第 1.1 节控件表描述现场；第 2 节才是目标。二者差异必须能在第 3 节找到，不允许只写一句「以第 2 节为准」。
- 第 1.3 节「现场 5 个模型调用」是旧管线；你的裁决是路线 LLM-A/B/C/D、学习双轨、问博主拆问+判定等。
- 账号菜单和设置页都会有夜间模式、退出登录，重复但可接受。

---

## 附录 · path-lab

独立实验页，不在侧栏。**已确认：作为实验网页清理掉，不进产品。** 产品路线制定只走 Chat 全流程。
