# Include inside this application's /api/ location. JSON snapshots are large;
# streaming text/event-stream stays outside gzip_types and is flushed normally.
gzip on;
gzip_types application/json;
gzip_min_length 1024;
gzip_comp_level 2;
gzip_vary on;
gzip_proxied no-cache no-store private auth;
