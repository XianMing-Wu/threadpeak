# 路线页灯光恢复与间距保留

2026-09-08，按用户最新裁决将路线页文字扫光替换回知识脉络页共用的蓝色 Lamp。路线页单独使用紧凑间距，知识脉络页保持原布局。

浏览器检查使用本地 `4301/#paths?tab=example`，只读取编选示例和页面布局，不触发生成或修改账户数据。

- [改前尺寸](before.json)与[改后尺寸](after.json)：1440、390、320 像素视口下，顶部区域、标题和说明的 x/y/width/height 全部一致。
- 标题与说明间距保持桌面 8px、窄屏 6px；顶部留白保持桌面 16px、窄屏 12px。
- 灯光图层与知识脉络页完全相同，已移除文字扫光组件；路线页可向下滚动，三个视口无页面运行异常。
- 截图：[桌面](paths-1440.png)、[390px](paths-390.png)、[320px](paths-320.png)。

命令日志保留在本目录。`check` 和完整 `build` 被本轮改动前已存在的 `server/path-generation/staged-plan.ts:114,125` 返回类型错误阻断；独立 Vite 前端构建与 lint 通过。该记录只验证本地视觉改动，不代表真实 provider 或生产部署验收。
