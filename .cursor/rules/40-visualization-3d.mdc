---
description: "约束最终路线 3D 展示、知识画布交互，并禁止恢复已删除的图文资产。"
globs: "src/learning-v2/Graph.tsx,src/learning-v2/Document.tsx,src/learning-v2/tree.ts,src/learning-v2/NodeEditor.tsx,src/knowledge-canvas/**,src/pages/Session.tsx,src/components/Path3D.tsx,src/path-3d/**,src/vendor/learning-path-3d/**,src/pathDocument.ts,packages/contracts/src/host-subject-flow.ts,packages/contracts/src/validate-renderer-document.ts,packages/contracts/src/path-document.ts"
alwaysApply: false
---

# 3D 与知识画布

可见目标以 [product.md](../../docs/product.md) 第 2 节的第 3、5、7、8 项与第 4.1 节为准。路线结构与学习卡片树合同分别由 10-path-generation.mdc、20-knowledge-lifecycle.mdc 和 [agents.md](../../docs/agents.md) 负责。

路线 3D 与知识画布是两件事，不能因为都可视化就共用数据或失败 fallback。产品已删除图文模式：不得恢复入口、失败页、`src/visuals/` 或 `vendor/charts/`，也不得用预写图冒充成功。

## 已删除的图文资产

- 不得把图表资产接到首页、Chat 或学习发送，也不能冒充用户请求成功。
- 不得用 timer、固定图、旧 BasisVisual、fixture frames、普通回答或知识图截图冒充图文成功。
- 不得新增任意 HTML/JavaScript、动态组件、工具循环或图文 schema 并称为正式合同。

## 3D 路线

1. 3D 只消费 R4 最终路线经过 3D 文档校验后的结果；R2 探索 JSON 不能直接渲染。
2. 校验失败的“我的路线”不发布、不进入列表、不进 3D；不得拿示例路线或残缺文档顶替。
3. carrierId/conceptId 与边引用必须来自同一份已校验路线；renderer 不补节点、不改边、不决定学习语义。
4. 显式边只表示推荐流转和分叉/汇合，不是解锁条件。所有节点从第一次打开起都可进入。分叉表示可以并列同时学，两路下一步接到同一个节点；同一分叉的两路之间不能再写直边，否则宿主平行边会和作者边撞在同一对节点上。
5. “走到这”只移动角色或播放动画，不创建 conversation、不生成首轮、不建知识图。
6. “进入学习”必须带上本路线中合法的 conceptId；缺少或不属于路线时不跳转。
7. 从学习会话返回 3D；3D 的返回统一到路线列表，不按来源回 Chat 或首页。
8. 每条路线再次进入时恢复自己的“上次位置”。产品只把 renderer 按 document 写出的不透明 progress 交回；有 progress 时 continue，否则 reset。不得在产品代码里解析成角色坐标、镜头或节点字段，也不得把位置当成学习进度。renderer 内部信封分别保存浏览位置与最近学习概念；宿主不得拆解。恢复必须先于导航初始化与画面揭示。最近学习概念用紫色标记，普通移动不覆盖；开始学习先保存概念返回位置再导航。
9. 3D 交互位置不是学习进度，不锁节点，也不能推导“完成/复习/错题”状态。

## 知识画布与文档

- 按 [学习生命周期规则](20-knowledge-lifecycle.mdc) 渲染单父卡片树，正交连线，不允许多卡汇入一卡。根为概念，后接文章和问答卡。
- 画布、文档、研究共用聊天。文档编辑定位到对应卡，原始文章与编辑副本分开。
- AI 和问博主在卡后新增，配色/同级/子级/撤销重做沿用用户认可的 ux-ui。操作权限、来源及树校验在服务端执行。
- 相机、缩放和展开状态不改语义；节点选择用于引用材料。切换视图不取消后台任务。

## 宿主生命周期

- 缓存每次反序列化会产生新引用；按内容判断是否回写，不能由渲染 effect 形成无限更新。
- 进度读取可取消，每次异步挂载独立拥有 DOM 和实例；旧实例迟到只能销毁自己。场景退出释放显卡上下文，恢复不重生成路线。
- 正式路线使用持久资源 ID 区分列表、导航与位置；旧 document.id 只在唯一匹配时用于位置兼容读取，不能混用重复文档的两个发布实例。

## Vendor

src/vendor/learning-path-3d 是同步产物，不直接手改 bundle、source map 或 GLB。若必须更新，从明确上游版本经同步流程生成，并验证公开入口、资源完整性和许可证；生成物不能包含密钥、provider URL 或用户数据。

- 场景阴影尺寸变化必须成对释放主贴图、深度纹理和模糊中间贴图，并受 GPU 尺寸上限约束；跨桌面/窄屏尺寸和反复进入后不得出现整场景变暗。

## 最低验证

路线分叉汇合与 renderer 校验、任意节点学习入口、返回关系保持正确。知识图验证单父树、画布/文档对应编辑、问 AI/博主挂卡与新对话保留，不能继续按旧批注语义验收。
