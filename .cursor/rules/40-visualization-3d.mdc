---
description: "约束最终路线 3D 展示、知识画布交互，并禁止恢复已删除的图文资产。"
globs: "src/chat/**,src/pages/Chat.tsx,src/knowledge-canvas/**,src/pages/KnowledgeCanvas.tsx,src/pages/Session.tsx,src/components/BasisVisual.tsx,src/components/Path3D.tsx,src/path-3d/**,src/vendor/learning-path-3d/**,src/pathDocument.ts,apps/web/**/path-3d/**,apps/web/**/knowledge-map/**,packages/surface-catalog/**,packages/path-3d-host/**,packages/contracts/visualization/**,server/visualization/**"
alwaysApply: false
---

# 3D 与知识画布

可见目标以 [as-implemented-logic.md](../../as-implemented-logic.md) 第 2.3、2.5、2.7、2.8 和 4.1 节为准。路线结构与 G1/G2 合同分别由 10-path-generation.mdc、20-knowledge-lifecycle.mdc 和 [agent-specs.md](../../agent-specs.md) 负责。

路线 3D 与知识画布是两件事，不能因为都可视化就共用数据或失败 fallback。产品已删除图文模式：不得恢复入口、失败页、`src/visuals/` 或 `vendor/charts/`，也不得用预写图冒充成功。

## 已删除的图文资产

- 不得把图表资产接到首页、Chat 或学习发送，也不能冒充用户请求成功。
- 不得用 timer、固定图、旧 BasisVisual、fixture frames、普通回答或知识图截图冒充图文成功。
- 不得新增任意 HTML/JavaScript、动态组件、工具循环或图文 schema 并称为正式合同。

## 3D 路线

1. 3D 只消费 R4 最终路线经过 3D 文档校验后的结果；R2 探索 JSON 不能直接渲染。
2. 校验失败的“我的路线”不发布、不进入列表、不进 3D；不得拿示例路线或残缺文档顶替。
3. carrierId/conceptId 与边引用必须来自同一份已校验路线；renderer 不补节点、不改边、不决定学习语义。
4. 显式边只表示推荐流转和分叉/汇合，不是解锁条件。所有节点从第一次打开起都可进入。分叉表示可以并列同时学，两路下一步接到同一个节点；同一分叉的两路之间不能再写直边，否则宿主平行边会和作者边撞在同一对节点上。
5. “走到这”只移动角色或播放动画，不创建 conversation、不生成首轮、不建知识图。
6. “进入学习”必须带上本路线中合法的 conceptId；缺少或不属于路线时不跳转。
7. 从学习会话返回 3D；3D 的返回统一到路线列表，不按来源回 Chat 或首页。
8. 每条路线再次进入时恢复自己的“上次位置”。产品只把 renderer 按 document 写出的不透明 progress 交回；有 progress 时 continue，否则 reset。不得在产品代码里解析成角色坐标、镜头或节点字段，也不得把位置当成学习进度。字段形态仍未裁决。
9. 3D 交互位置不是学习进度，不锁节点，也不能推导“完成/复习/错题”状态。

## 知识画布

- 画布展示的是该概念唯一图和根，以及 G1/G2 两路都成功后提交的新卡；布局和点击都不能生成或改变语义。
- 根标题永远是概念名，根正文永远是固定首轮。没有合法首轮/根时不能发明占位根。
- 画布与该概念最新对话使用同一份消息记录。画布发问、流式输出、划选和问博主要实时写回，回到对话无需再生成。
- 从学习页或知识侧栏进入画布，右上角都显示“回到对话”：有对话回最新一条；没有则新建对话并继续使用同一根。
- 画布顶栏返回清掉所选概念，回该知识的概念列表。
- 放大、缩小、重置、拖动画布、拖卡片、重新排列只改视图；不能改宿主、节点关系或正文。
- 点击节点只查看节点，不选择下次提问宿主。显式引用才选对应卡；无引用时由最近一次成功 LLM 回复卡作宿主。
- 边上的数字打开逻辑说明，不创建或修改边。

## 批注展示

- 问博主的真实作者结果与刘看山直答都只是锚定原文的批注，不是知识节点。
- 节点正文上只显示批注标记；点击标记后在节点右侧打开同等大小的展示卡，点击卡外关闭。
- 作者批注卡使用本次 evidence 的真实姓名、总结、链接；刘看山卡明确标注“刘看山直达/不是博主身份”。
- 根与后续卡批注跨新对话保留；切换 conversation 不能清掉图上标记。

## 视图与语义

- 相机、缩放、拖拽位置、展开状态和批注卡开合都是视图状态，不是路线/知识/作者事实。
- 布局可以重新计算坐标，但不能合并语义不同的节点、改变 predecessor/successor/parallel 或选择宿主。
- 颜色不能成为唯一状态编码；关键按钮需有键盘、可见焦点和可读名称。

## Vendor

src/vendor/learning-path-3d 是同步产物，不直接手改 bundle、source map 或 GLB。若必须更新，从明确上游版本经同步流程生成，并验证公开入口、资源完整性和许可证；生成物不能包含密钥、provider URL 或用户数据。

## 最低验证

- 产品没有图文入口；不得恢复 `src/visuals` 或图表 vendor 资产。
- R4 文档校验、分叉/汇合、所有节点可进入、非法 concept 不跳转。
- “走到这”无学习副作用；学习页→3D→路线列表返回正确。
- 重新进入路线能恢复同一 document 的 renderer progress；测试不解析或冻结未裁决的位置字段。
- 画布与最新对话实时同步，任何入口都有“回到对话”。
- 点击/拖动/重排不改宿主或语义；只有引用决定宿主。
- 批注卡不是结构节点，跨新对话仍存在。
- 键盘、焦点和低视口操作使用同一份真实数据。
