---
description: "约束 R1–R5 路线制定、澄清题轮次、附件、最终路线 JSON、发布与 3D handoff。"
globs: "src/path-planning/**,src/path-3d/**,src/pathDocument.ts,src/pages/Chat.tsx,src/pages/Home.tsx,src/pages/Collections.tsx,src/workspace/**,src/components/Path3D.tsx,src/chat/**,apps/web/**/path-planning/**,apps/web/**/path-3d/**,packages/contracts/path/**,packages/path-3d-host/**,server/path-generation/**,server/ordinary-chat/**"
alwaysApply: false
---

# 路线制定 R1–R5

目标流程以 [as-implemented-logic.md](../../as-implemented-logic.md) 第 2.6、2.7、2.8、4.1 节为准；每个 Agent 的输入、压缩、系统提示词和输出以 [agent-specs.md](../../agent-specs.md) 第 0 节和第一节为准。旧的一次 CandidateSet、固定五角检索和纯 TypeScript 剪枝不是目标。

## 固定流程

~~~text
用户目标
  → R1 拆成 4–5 条、最多 5 条知乎问法
  → R-S 最多 2 路并发搜索知乎（4–5 问按角度空格拼串）
  → R2 汇总全部可能载体、概念和争议
  → R3 生成第 1 轮 1–3 道小白选择题
  → [R3b 处理题中追问，最多到第 3 轮]
  → 当前 active 题组全部选完后自动进入 R4（不出现「生成完整路径」）
  → R4 生成最终路线 JSON，不再搜索
  → 3D 文档校验
  → 发布一条新的“我的路线”
~~~

1. 生成失败后的“重试”从同一目标的 R1 整段重跑，不从中间 Agent 拼接旧结果。
2. 再次点击路线制定会重新跑完整流程并发布另一条路线，不覆盖上一条。
3. 路线发布后 Composer 仍在；普通发送走 R5。要再制定一条新路线，回到首页发送并重新走 R1。首页发送一律进入路线制定。
4. 路线的任何阶段都不能创建概念首轮、conversation、KnowledgeGraph 或根节点。
5. R-S 是搜索步骤，不是模型 Agent；R4 后不再检索。

## R1 / R-S / R2

- R1 必须同时覆盖正常学法与坑/争议两类角度；结果 4–5 条且最多 5 条。
- R-S 把 R1 的 4–5 问按角度用空格拼成最多 2 路并发检索，结果按 queryId 保留 authorId、evidenceId、API 总结和真实链接。
- R2 必须读取所有检索分组与附件，不得只读排名第一的结果。
- R2 输出是中文动态键的探索对象：顶层键是载体名，第二层键是概念名，每个概念只含“争议”布尔值。
- R2 探索对象不是最终路线，不得提前出现 routeId、节点数组、边或学习顺序。

## R3 / R3b 题组

1. R3 只帮用户把精准学习目标说清楚，不考知识，也不把探索里的专题名拿来让用户取舍；输出 1–3 道大白话题，每题至少两个选项。
2. 初始题组 round=1、status=active。每套题都有 round=1..3 与 active/superseded。
3. 用户没答完时可以追问。R3b 判断：
   - 与当前题无关：解释应先答完，返回 continue_current，不换题；
   - 相关且当前轮小于 3：解释后返回 replace_questions，产生下一轮 1–3 道新题；
   - 当前已是第 3 轮：可以解释，但不再换题。
4. 换题后旧题保留在对话中、变灰且不可再点；旧题和已选 optionId 继续进入 R4。
5. 不同轮选择冲突时较新轮优先。当前 active 题组全部选完后自动调用 R4，不得再要用户点「生成完整路径」。
6. 问题、选项和题组输出必须通过 agent-specs.md 的联合结构验证；不合格时按 0.8 由同一 Agent 自我修复，不能用自由文本猜测分支。

## R4 最终路线

R4 输出固定字段的版本 1.0 路线对象，至少包含：

- routeId、title；
- carriers：稳定 id、title、description；
- concepts：稳定 id、carrierId、title、hasDispute、detailedDescription、attachmentSourceIds；
- carrierEdges 与 conceptEdges：稳定 id、from/to 和 reason；
- entryConceptIds 与 terminalConceptIds。

所有引用必须闭合、全部 ID 在单份路线内唯一、图无环且所有概念从入口可达。结构或图校验不合格时按 0.8 由同一 R4 自我修复，不能用自由文本猜测节点或边。数组顺序只用于稳定展示；显式多出边表示分叉，多入边表示汇合。分叉表示并列同时学，两路下一步接到同一个节点，一路不能先直达终点。

每个概念必须有 detailedDescription，说明首次讲解偏向和要解决的问题。附件相关信息只消化进对应概念的 detailedDescription，并保留真实 attachmentSourceIds；开始学习后不再携带附件原件。

每个载体通常包含 2–3 个概念，确有必要时可以只有 1 个；不能再用旧实现强制每个载体只有一个概念。

## 附件与 500k 预算

- 附件只来自首页同一次发送，格式仅 pdf/md/txt。
- R1、R2、R3、每次 R3b、R4 都携带这次路线请求的附件。
- 整次调用总量小于 500k 不压缩；达到或超过 500k 才压缩。
- 非附件部分小于 300k：先做一次有目标额度的附件压缩；仍超预算再压非附件，不重复压附件。
- 非附件部分大于等于 300k：压非附件，不动附件。
- 继续收紧允许压缩的正文直到整次调用小于 500k；不返回“上下文过长”产品错误。
- sourceId、query/evidence/author ID、题目轮次、状态、选项和路线引用关系不能被摘要改写。

## 发布、列表与 3D

- 只有最终路线通过 3D 文档校验后才发布并出现在“我的路线”；半成品不显示。校验必须包含 3D 宿主图约束：独立无概念终点、全员可达、分叉/汇合要有 flowGroup；同一分叉的两路之间不能再写一条直边（宿主会自己画平行边）；分叉两路下一步接到同一个节点，不能一路跳到终点。
- 路线列表空态回首页，不代写、不代发。
- 3D 中所有节点从一开始都可进入。边是推荐流转，不是解锁条件。
- “走到这”只播放/移动，不创建会话、不建知识；“进入学习”必须携带本路线内合法 conceptId。
- 学习会话返回 3D；3D 返回统一去路线列表。
- 每条路线要恢复“上次 3D 位置”。只把 renderer 不透明 progress 按 document 交回，不得自行冻结角色、镜头或节点字段。
- 示例路线必须始终标明示例；用户路线校验失败时不得拿示例替代。

## path-lab

path-lab 与 `/api/paths/generate` 已从源码删除。不得重新加入侧栏、产品路由、用户写链或“我的路线”。

## 最低验证

- R1 数量/角度、R-S 最多 2 路并发且问法空格拼串、R2 动态探索对象、R3/R3b 三轮上限、R4 固定路线结构。
- 附件五个阶段都携带、300k 两分支、500k 总预算与无超长错误分支。
- 旧题保留/变灰/不可点，当前题组选完自动进 R4，R4 读取全部已选答案且新轮优先。
- 路线校验失败不发布；发布与 3D 不产生任何知识写入。
- 分叉/汇合可渲染、所有节点可进入、返回路线列表、非法 concept 不跳学习页。
- 路线发布后 R5 可继续，重新制定新增路线；path-lab 不再可达。
