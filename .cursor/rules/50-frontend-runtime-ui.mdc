---
description: "约束 ThreadPeak 前端页面、Composer、跨页状态与已裁决交互。"
globs: "src/**/*.ts,src/**/*.tsx,src/**/*.css,packages/runtime-store/**,packages/api-client/**"
alwaysApply: false
---

# 前端运行时

[最新产品目标](../../docs/product.md)与[Agent 合同](../../docs/agents.md)拥有行为，ux-ui 是已确认视觉与交互参考。

- 真实数据从服务端资源恢复。页面卸载、切换研究/画布/文档、浏览器断网不取消后台工作；显示重连并保留内容。提交成功前不清输入、不藏选择题。
- 首页品牌固定文字使用本地矢量字形，首次可见即为艺术字，不依赖外部字体或系统回退。
- 首页附件支持 pdf/md/markdown/txt 和显式导入的知乎授权收藏夹/公开创作，服务端解析。首页默认路线，普通 Chat 走 R5；没有发送上下文进入 404。无图文入口，无首页问博主模式。
- 路线最多三轮，已答题和被替换题以可展开的状态胶囊只读保留；新题组 2–4 题且显示提问原因，补查期间即可答题；全答且资料就绪自动 R4。 答题后的确认、安排路线与发布状态位于已答胶囊下方；整段使用统一行间距。继续同一任务和重新制定分开。
- 学习是文章/聊天两栏可调宽；研究与知识脉络共享当前对话及思考深度。新对话清空聊天、保留文章/树，旧聊天可重开。
- 画布正交单父树，编辑跳文档对应位置，支持添加同级/子级、颜色/描边、复制/删除/撤销。问 AI 与问博主为选卡后的悬浮输入，成功在对应卡后长新卡。
- 草稿暂显，完成后正式段落和卡片一起出现。文档与图共用编辑副本，原始文章不变；冲突保留本地编辑供明确选择，不能静默覆盖。
- 深度只有快速/深度，默认快速，每次服务端接收时冻结；切换只影响后续操作。离开学习/图界面重置快速。
- 学习回 3D，3D 回路线列表；renderer progress 不透明按路线保存在服务端，本地缓存仅回连前备用。
- 博主网络先查学习来源，再查新证据，最多三位；同相关性才参考可撤销的主题反馈。公开文章观点不能标作本人收到提问后的回答。
- 登录页提供知乎账号与游客两个明确入口，游客需服务端签发会话，同样保存进度；游客隐藏所有 OAuth／收藏夹／授权资料入口，保留文件及全知乎／全网搜索。账号改变隔离浏览器旧投影，秘密不得放客户端。未接通身份提供方不可假称知乎登录成功。
- 验收真实文章、题组刷新、编辑定位、浮动提问、历史、新对话、窄屏、焦点及返回链；内部 schema/Agent 错误不作为产品流程。

- 首页范围图标紧邻上传图标左侧；全知乎/全网保留，已登录知乎时下方显示仅知乎收藏夹及名称，游客不读取或展示该区；附件位于输入框内顶部紧凑行。OAuth 演示与收藏按本轮授权开放，但须标记演示。公式图片不静默隐藏。

- 示例知识与示例路线的学习入口共用当前 LearningWorkspace，不再路由到旧卡片画布；示例标识、本地编辑与真实持久资源隔离。

- 作者输入整合请教目的、学习记录与深度，使用静态边框；补充背景为悬浮编辑。浅粉／浅紫交互底统一白色，选中仍可辨识。评论完整展示 API 返回内容，不展示总数、覆盖说明或重复评论链接；无评论不显示。移除数据与推荐设置入口，作者卡仅预览一篇文章；详见两份源文档的本轮补充。

- 访谈每题三个建议加“我想自己说”，一次展开一个待回答问题。自定义文本独立原样保存，不映射虚构 optionId；提交成功才收起，刷新与已过期题保留实际回答，选项/自由补充同等有效。

- 提问输入的发送/停止复用统一控件与状态，启用态黑底白图标、禁用态中性灰；资料足迹区分来源卡、讲解和问题，内部卡片链接定位具体节点，反馈写明资料与概念。
