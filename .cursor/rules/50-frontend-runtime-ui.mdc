---
description: "约束 React 前端 runtime、状态、交互、无障碍和模块依赖风格。"
globs: "src/**/*.ts,src/**/*.tsx,src/**/*.css,apps/web/**,packages/runtime-store/**,packages/api-client/**,packages/design-system/**"
alwaysApply: false
---

# 前端 Runtime、React 与交互

前端只负责 command/query、协议投影、稳定 store 和受控 view。页面可展示领域状态，不能发明或拥有领域事实。

## 数据流

```text
API/NDJSON/SSE → api-client decoder → context projector
  → RuntimeStore immutable snapshot → selector → feature UI
UI action → typed command method → api-client
```

- 每个协议/context 只有一个共享 decoder/projector；页面和组件不得直接 `fetch`、`EventSource`、读取 `ReadableStream` 或用 `setState` 拼领域消息。
- ThreadPeak 自有 `RuntimeStore` 拥有稳定 `getSnapshot/subscribe`、immutable snapshot、selector equality、inflight UI state 和 teardown。
- assistant-ui 若使用，只能作为 ExternalStoreAdapter/UI shell；它的 SDK type 不能进入 contracts、store、server 或 domain。
- 组件卸载时取消订阅；迟到 event 按 resource/request/sequence 拒绝。重复 event 不改变结果，gap 触发 replay/GET。
- `localStorage/sessionStorage` 只允许主题、density、reduced-motion、未提交 composer draft 与无敏感缓存；route、path、conversation、history、graph、author network 事实必须来自服务端。

## Feature 与页面边界

| Feature | 只负责 | 禁止 |
| --- | --- | --- |
| `path-planning` | path command/stream projection/result card | 建 knowledge、保存 3D 进度 |
| `path-3d` | validated document/wire handoff | 生成路径、首答或 graph |
| `concept-learning` | entry resource、conversation tree、mode output | 直调知乎/LLM、写 graph/network |
| `knowledge-map` | graph card/snapshot/revision | 从消息/layout 推导语义 |
| `author-search` | query/stage/0–3 people | network mutation、conversation answer |
| `author-network` | owner-scoped snapshot/controlled graph | 搜索输入、写 knowledge graph |
| `history` | conversation summary/exact reopen | 缓存正文为真相、重跑模型/projector |

- `pages/` 只组合 feature、路由参数和 layout；不得持有 provider、协议解析、业务状态机、数据库/graph mutation 或跨 feature mutable state。
- 「搜索博主」与「博主网络」可在一个页面并列，但保持两个 feature model/API。
- `concept-learning` 可以渲染 AskAuthor result，但不能 import author-search page state。

## UI 状态

- Composer 模式只接受 `'' | route | visual | authors`；浏览器草稿中的未知值必须在读取边界归一为空模式，不能直接进入组件状态。
- 每个服务端能力使用 discriminated union 表达实际可达的 `idle/loading/streaming/ready/empty/recoverable-error/terminal-error/cancelled/reconnecting` 子集；禁止多个 boolean 组合不可能状态。
- 首次概念页按真实顺序显示 initial answer generation → initial ready → graph bootstrapping → ready。再次进入/新对话不得重播首答生成；graph 失败不能遮蔽首答。
- History reopen 恢复精确 committed snapshot，不触发模型、作者筛选或 graph rebuild。
- 旧 document/revision 在新请求失败时继续显示；禁止先清 UI 再发请求。

## React 约束

- `useEffect` 只同步外部系统、订阅和生命周期。纯派生值使用 selector/`useMemo`；用户动作调用 command method，不在 effect 串业务 pipeline。
- Component props 只传 view data 与 command callbacks；不传 DB row、provider response、Fastify request、mutable store 或 SDK object。
- 组件不得直接 mutate store/tree；所有 mutation 经 store API 和 server command。
- 使用 named export；React 组件文件 `PascalCase.tsx`，hook `useXxx.ts`，业务 TS `kebab-case.ts`。
- 普通手写 TS/TSX 目标不超过 400 有效行。超限旧文件进入机读 debt allowlist，不能增加净行数或新职责。

## 样式与可访问性

- 全局 CSS 只放 reset、token、font、shell 和必要 portal；feature 样式局部化，禁止继续堆进单个 `styles.css`。
- Design system 只含无业务 primitive、token、focus/motion/a11y 基线；不能拥有 route/fetch/store/领域状态。
- 控件使用真实 `button/link/input`，支持 keyboard、visible focus、Escape/焦点返回、ARIA name、reduced motion；颜色不是唯一状态编码。
- 主题、density、reduced-motion 是前端偏好，不改变领域合同。
- 侧栏、画布和低高视口修复必须验证 `clientHeight/scrollHeight/scrollTop`、遮挡和焦点，不只看截图。

## 前端完成证据

- 运行 typecheck、unit/contract tests 和 build；源代码 regex 不能代替运行时验证。
- 受影响旅程使用真实浏览器覆盖基线 `1280×720` 与实际受影响窄/低视口、keyboard、focus、reduced motion、断线/重连。
- 确认浏览器只访问同源 API/BFF，不知道数据库、provider 或开发端口。
