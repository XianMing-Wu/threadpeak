---
description: "为产品代码与工程配置定义两份目标文档的权威边界、领域职责和 Agent 编排约束。"
globs: "src/**,packages/**,server/**,scripts/**,Dockerfile,compose.yaml,deploy/**,package.json,tsconfig*.json,vite.config.*,vitest.config.*,eslint.config.*"
alwaysApply: false
---

# 当前架构与权威边界

当前用户裁决最高；[产品现状与目标](../../docs/product.md)区分现场、目标和上线缺口，[Agent 合同](../../docs/agents.md)拥有提示词、上下文、压缩与结构。[工程设计](../../docs/engineering.md)记录本次获授权的持久化方案；不再把这些工程选择列为需要逐条追问的旧空位。

- 默认入口是 server/durable。固定流程由程序编排，模型只生成当前步骤的受约束结果；不增加统一总控、语义 judge 或第二个修复 Agent。MCP 是可选适配层。
- 路线：R1/R-S/R2（非思考提取至多四名称）→ 按两条一批补查与 R3 访谈并行 → R4 直接规划；主动追问 R3b；普通问答 R5。发布路线不创建知识。
- 学习：L-search-plan → 2–6项按两条一批搜索 → L-source-select → L-answer连续教学。先保存可读文章，再原子提交回答和卡片。
- 问博主：A-card-plan → 2–4项按两条一批检索 → A-card-select；有候选至少选相对最佳的一位，每位单独一张卡，绝对零候选如实说明且不转直答。结果是卡片，不是旧批注。博主搜索 N0 来源网络→N1/N-S 新证据→N2 适配，主题偏好只在同相关性内排序。
- 共享合同位于 packages/contracts；服务端拥有资源、版本、任务和数据权限，浏览器只有可丢弃的投影。示例不能填补用户请求失败。
- 一个知识卡只有一个父卡，每段只依据一个 basisId；路线的 3D DAG 仍可汇合。数据引用回查真实输入 ID，不由布局推导语义。
- 原文与压缩/用户编辑副本分开；输入先持久接受，完成写入事务，停止和迟到响应受租约/fence 约束。
- 学习搜索2–6项、问博主2–4项，两条一批且等前批收齐；不调用直答，问法不能截断或丢弃，过长由同一规划 Agent 修复。
- implemented、真实 provider integrated 与 production-ready 分开。静态测试、文件存在、录屏都不能证明生产无故障。

- 搜索范围随路线与首次学习继承；仅收藏夹不调用外部检索。全网每个业务搜索组内顺序调度知乎/全网两个检查点；仅知乎来源能进入作者网络。
