---
description: "为产品代码与工程配置定义两份目标文档的权威边界、领域职责和 Agent 编排约束。"
globs: "src/**,apps/**,packages/**,server/**,package.json,tsconfig*.json,vite.config.*,eslint.config.*,pnpm-workspace.yaml"
alwaysApply: false
---

# ThreadPeak 架构核心

本规则只定义跨域边界。可见行为以 as-implemented-logic.md 第 2 节与第 4.1 节为准；Agent 的上下文、压缩、系统提示词和输出结构以 agent-specs.md 为准。现场源码只能证明当前实现，不能覆盖目标。

## 先分清三种内容

| 内容 | 怎么使用 |
| --- | --- |
| 目标语义 | 按两份源文档实现，不从旧源码、fixture 或旧算法反推 |
| 当前现状 | 用 as-implemented-logic.md 第 1 节和现场命令定位要替换的旧链 |
| 未裁决空位 | 保持显式失败或最小内部封装，不自行冻结产品 schema、事务或 UI 行为 |

不得把“规则已经写了”报告成 implemented，也不得把测试通过报告成用户目标已全部实现。

## 领域职责

| 领域 | 只负责 | 不能负责 |
| --- | --- | --- |
| 路线制定 | R1–R4、题组轮次、路线 JSON、校验与发布 | 首轮、conversation、知识图、作者网络 |
| 普通 Chat | R5 当前 Chat 的普通回复 | 自动重开路线流程、学习图写入 |
| 概念首次学习 | L0a/L0b、唯一首轮、确定性唯一根 | 重跑首轮、用旧示例代替真实结果 |
| 学习追问 | G1/G2 并发、对话回复与条件性长图 | 点击节点选宿主、跨历史对话取上下文 |
| 问博主 | A1–A3、真实作者证据、刘看山回退、批注 | 无划选触发、把批注变结构节点 |
| 博主搜索 | N0–N2、高权→低权→知乎、最多 3 人 | 返回回答、虚构或重复作者 |
| 3D/画布 | 校验后展示、镜头与可见交互 | 发明路线节点、通过布局改语义 |
| 身份/附件/provider | 服务端身份、附件来源、真实外部调用 | 浏览器持有密钥、fixture 冒充成功 |

跨域交接必须使用稳定 ID 和经过运行时校验的结构。引用已有对象时只能引用输入中的 ID；只有合同明确要求新建 ID 的 Agent 才能生成新 ID。

## Agent 编排边界

产品中的主调用角色只有 agent-specs.md 已列出的：

- 路线：R1、R2、R3、R3b、R4、R5；
- 首次学习：L0a 三路、L0b；
- 学习追问：G1、G2；
- 问博主：A1、A2、A3；
- 博主搜索：N1、N2。

R-S、A-S、N-S 是知乎搜索，N0 是 Graph RAG，3D 校验、提示题展示、结果汇合、根创建和批注展示都是确定性步骤，不得包装成额外 LLM Agent。

禁止增加“统一总控 Agent”、第二次语义 judge 或循环修复 Agent。确需新增模型调用时，先由用户裁决并同步 agent-specs.md。

## 数据和执行边界

1. 浏览器只提交用户动作、稳定资源 ID、当前模式和文件；不得直连知乎或 LLM provider。
2. 服务端按 Agent ID 构造上下文、执行压缩、调用真实 provider、校验输出，再交给确定性步骤。
3. JSON 输出先按对应结构验证；解析失败是该 Agent 失败，不能用启发式字段、旧 fixture 或自由文本补成成功。
4. authorId/evidenceId、附件 sourceId、route/concept/node/message/conversation ID 不得靠名称或数组下标猜测。
5. 原文与压缩副本分离。压缩只改变本次模型上下文，不覆盖用户消息、附件、证据、节点或批注原文。
6. 页面和画布只能提交并呈现既定流程的结果，不能根据点击、布局或本地缓存自行创造路线、首轮、知识节点或作者身份。
7. 示例路线/知识与“我的”数据始终分开；示例不能进入真实请求、写入网络或填补 provider 失败。

## 未裁决架构不得写死

两份源文档尚未决定具体持久化表、数据库类型、事务拆分、事件协议、幂等键、后台 job、取消/超时/刷新恢复和 3D 位置字段。实现这些内容前必须先补相应产品/合同裁决；不得仅凭旧规则恢复 PostgreSQL、CAS、SSE、outbox、GraphProjectionPipeline 等为既定事实。

可以保留不改变产品语义的接口隔离、runtime schema 校验和测试替身，但不能声称它们已经解决永久保存或崩溃恢复。

## 成熟度

| 标签 | 能说明什么 |
| --- | --- |
| prototype | 只证明原型旅程或 fixture |
| specified | 两份文档已经写明目标 |
| implemented | 现场代码完成该目标并有相应自动测试 |
| integrated | 真实前后端/provider/持久化纵向跑通 |
| production-ready | 未裁决空位已补合同，安全、恢复、迁移、性能和 live gate 都有当前证据 |

文档修改最多证明 specified；目录、类型检查、mock、截图或历史通过数不能越级。

## 最低验证

- 检查目标代码没有复制或改写 Agent 提示词、枚举和输出字段。
- 检查跨域引用使用稳定 ID，用户请求没有 fixture/示例 fallback。
- 检查路线不写知识、问博主批注不长结构节点、刘看山不进作者网络。
- 检查未裁决空位没有被悄悄实现成唯一生产合同。
