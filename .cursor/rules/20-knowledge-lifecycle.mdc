---
description: "约束来源卡片树、L-answer 工具范围、对话和文档编辑同步。"
globs: "src/pages/Session.tsx,src/learning-v2/**,server/durable/**,packages/contracts/src/learning-v2.ts,src/pages/NotFound.tsx,src/pages/KnowledgeCanvas.tsx,src/pages/Collections.tsx,src/learningSession.ts,src/history.ts,src/resolve-history-reopen.ts,src/workspace/nav.ts,src/workspace/store.ts,src/knowledge-canvas/**,src/session/**,src/components/Shell.tsx,src/components/AnnotatedText.tsx,src/components/SelectionToolbar.tsx,src/components/Composer.tsx,src/components/AskAuthorsPrompt.tsx,apps/web/**/concept-learning/**,apps/web/**/knowledge-map/**,apps/web/**/history/**,packages/contracts/knowledge/**,server/knowledge/**,server/first-learning/**,server/follow-up/**"
alwaysApply: false
---

# 学习卡片树生命周期

唯一目标见 [现状清单](../../as-implemented-logic.md) 第 2 节与 [Agent 合同](../../agent-specs.md) 第二、三节；工程运行边界见 [重构记录](../../docs/backend-rebuild-2026-09-06.md)。

- 首次概念名称消息 → 三路限定概念的搜索 → 收齐筛选 → 文章与根持久保存 → 三角度直答 → L-answer。
- 概念唯一根，文章及本条路线资料为一级子卡，每段只一个 basisId、每卡只一个父卡，禁止汇合、循环与伪造 ID。路线 3D DAG 不受此限制。
- 不再运行 G1/G2；模型通过 append_cards(after=C编号) 逐段指定单父卡，每操作恰好一张新卡；read_card_scope 固定本次材料，服务端回查该材料版本中的 evidence、解析编号和父卡，再原子提交回复和卡片。不能将全部段落打包默认挂 C1，也不能为分散编号伪造来源。
- 选中卡片决定提问材料；无选中默认全部文章及路线资料；资料详情默认选当前资料。用户问题必须非空。
- 新对话归档旧聊天、当前为空，保留文章、根和所有卡。切换研究/画布/文档不取消任务。
- 编辑在文档对应位置发生，保存同一卡的用户副本，禁止修改原始文章总结/身份/来源。
- provider 不完整不产生正式卡；停止与晚到提交由持久事务和 fence 裁决，不能靠前端 mounted 标记代替。
- 验证重开、并发、ID 绑定、取消、单父卡、归档与编辑版本冲突，测试替身与真实 gate 分开。

- 公式共用 MarkdownMath；文档阅读与源文本编辑分开，保存不用渲染 DOM 反推源码。长卡片展开后正文可滚动，工具栏和收起按钮必须保留在卡片内。

- 首页加入的文件与显式导入的收藏资料用于路线生成并继承至每个概念，已有资料可先阅读。PDF 卡用总结，文本卡保留正文；材料与概念不相关时不强行引用，文件不能冒充知乎作者。

- 本轮范围继承覆盖默认知乎检索：全网额外收集站外资料；仅收藏夹跳过 L-search-plan/检索，三角度由模型只读资料完成。站外卡无作者身份，不进入作者网络。
