---
description: "约束来源卡片树、L-answer 工具范围、对话和文档编辑同步。"
globs: "src/pages/Session.tsx,src/pages/Collections.tsx,src/learning-v2/**,src/knowledge-canvas/**,src/session/**,src/history.ts,src/resolve-history-reopen.ts,src/workspace/**,src/components/Shell.tsx,server/durable/**,server/knowledge/**,server/agent-runtime/**,packages/contracts/src/learning-v2.ts,packages/contracts/src/learning-goal.ts,packages/contracts/src/node-edits.ts"
alwaysApply: false
---

# 学习卡片树生命周期

唯一目标见 [现状清单](../../docs/product.md) 第 2 节与 [Agent 合同](../../docs/agents.md) 第 2、3 节；工程运行边界见 [工程设计](../../docs/engineering.md)。

- 首次概念名称消息 → 依照概念四项总结设计2–6条互补问法，两条一批搜索 → 收齐筛选 → 文章与根持久保存 → L-answer连续教学，每块H2标题与真实引用。
- 概念唯一根，文章及本条路线资料为一级子卡，每段只一个 basisId、每卡只一个父卡，禁止汇合、循环与伪造 ID。路线 3D DAG 不受此限制。
- 不再运行 G1/G2；模型通过 append_cards(after=C编号) 逐段指定单父卡，每操作恰好一张新卡；read_card_scope 固定本次材料，服务端回查该材料版本中的 evidence、解析编号和父卡，再原子提交回复和卡片。不能将全部段落打包默认挂 C1，也不能为分散编号伪造来源。
- 选中卡片决定提问材料；无选中默认全部文章及路线资料；资料详情默认选当前资料。用户问题必须非空。
- 新对话归档旧聊天、当前为空，保留文章、根和所有卡。切换研究/画布/文档不取消任务。
- 编辑在文档对应位置发生，保存同一卡的用户副本，禁止修改原始文章总结/身份/来源。
- provider 不完整不产生正式卡；停止与晚到提交由持久事务和 fence 裁决，不能靠前端 mounted 标记代替。
- 验证重开、并发、ID 绑定、取消、单父卡、归档与编辑版本冲突，测试替身与真实 gate 分开。

- 公式共用 MarkdownMath；文档阅读与源文本编辑分开，保存不用渲染 DOM 反推源码。长卡片展开后正文可滚动，工具栏和收起按钮必须保留在卡片内。

- 首页加入的文件与显式导入的收藏资料用于路线生成并继承至每个概念，已有资料可先阅读。PDF 卡用总结，文本卡保留正文；材料与概念不相关时不强行引用，文件不能冒充知乎作者。

- 检索范围继承覆盖默认知乎检索：全网额外收集站外资料；仅收藏夹跳过 L-search-plan/检索，由模型只读资料直接组织连续教学。站外卡无作者身份，不进入作者网络。

- goalContext 从本账号路线带入概念搜索、筛选、首次及后续讲解；包含原始目标、真实访谈回答、最终成果与概念用途/深度/检验任务。新对话保留目标，显式选卡改变证据范围而不删除目的；未知旧目标不编造。

- 首学L-answer直接读取本轮已筛选文章及路线资料；检索、筛选和讲解沿概念的用途与深度，不能将局部学习扩成整门学科。学法差异与事实争议分开，未观察到分歧不等于证明不存在。

- 审阅全部来源不等于逐篇生成正文；围绕当前概念与目标去重，首次最多 8 段、追问最多 12 段。超限同一 L-answer 修复，不截断结果发布；版本与数学前提不能因来源有引文就免于核对。
- 首次文章筛选读取全部候选总结，通常3–6篇、最多8篇，不限制资料数量。概念learningSummary四项总结、目标和路线贯穿搜索/筛选/正文；没有L0a/A3及知乎直答调用，模型摘要只在实际LLM预算不足时触发。
