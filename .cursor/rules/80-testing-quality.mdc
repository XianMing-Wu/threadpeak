---
description: "定义与两份重构文档一致的测试矩阵、证据边界和完成标准。"
globs: "src/**,apps/**,packages/**,server/**,tests/**,catalog/**,qa/**,package.json,tsconfig*.json,vite.config.*,eslint.config.*,playwright.config.*"
alwaysApply: false
---

# 测试、质量与完成定义

测试的目标语义来自 [`as-implemented-logic.md`](../../as-implemented-logic.md) 第 2 节、第 4.1 节和 [`agent-specs.md`](../../agent-specs.md)。旧源码、旧 fixture 和已有测试只证明现场；若它们与目标冲突，应更新测试，不得用旧测试反向证明目标文档错误。

## 基本质量边界

- 外部输入和模型结构按共享 runtime schema 校验；不能靠 TypeScript assertion 把无效数据当有效。
- UI、服务端和测试共用相同字段、枚举及 Agent 输出结构，避免手写三套近似 type。
- 测试 fixture 必须明确标为测试数据；生产 composition 不能 import 或回退到 fixture。
- 不用源码关键词或固定延时作为唯一验证。行为、错误、部分成功和跨页恢复都要通过可执行测试或浏览器证据验证。
- 文档变化只证明规则一致；不能据此声称 runtime、真实 provider、持久化或 production-ready 已完成。

## 文档与规则检查

- 所有 `.mdc` 有完整 frontmatter、非空 globs、`alwaysApply: false` 且少于 500 行。
- 根 `AGENTS.md` 路由表与 `.cursor/rules/` 文件一一对应；相对链接可解析；Markdown 围栏、表格、标题、trailing whitespace 与 merge marker 检查通过。
- 检查每条产品不变量在根入口和 owning rule 中含义一致，尤其是 R2/R4 区别、500k/300k、首轮与根同一成功态、G1/G2 汇合、A1–A3、N0–N2。
- `agent-specs.md` 中每个 Agent 都应具备上下文获取、压缩算法、系统提示词和输出结构；非 Agent 步骤不得被误计为 Agent。

## 行为测试矩阵

| 范围 | 必须覆盖 |
| --- | --- |
| 路线 | R1 产 4–5 问；R-S 并行；R2 动态探索；R3/R3b 每轮 1–3 题且最多 3 轮；R4 稳定 ID、显式边、分叉/汇合及 renderer 拒绝；失败从 R1 重试；发布不建图；发布后 R5 |
| 附件/预算 | 仅首页 pdf/md/txt；路线各 Agent 可见；普通 Chat 只跟随本 Chat；学习无附件；总预算 `<500k` 不压，`>=500k` 按 `<300k`/`>=300k` 分支；持续压到预算内且不改原文 |
| 首次学习 | L0a 三路并行且方向分离；L0b 使用概念详细描述；任一路失败不成功；L0b 成功与唯一根同一终态；再次进入/新对话/刷新不重生成 |
| 学习追问 | 必须有问题；显式引用/默认最近回复/选择旧问题的宿主映射；点击无效；G1 一跳邻域；G2 当前对话全文；四种并发终态中只有双成功写卡，G2 成功/G1 失败只回话 |
| 问博主 | 先划选；A1 产 2–3 问；A-S 并行；A2 只从输入 ID 选 1–2 人；正常零人走 A3；provider 失败不走 A3；批注不进结构图；真实作者高权入网；刘看山不入网 |
| 搜索博主 | N0 高权后低权；不足 3 才 N1/N-S/N2；网络不可用报错、网络为空可查知乎；总数最多 3；不足时全部返回；去重；知乎新作者低权入网；刘看山永不出现 |
| UI/导航 | 首页无问博主快捷；芯片只填不发；快速/深度两档、默认与跨页范围正确；路线问题逐轮灰化；图文明确失败；学习/画布同步；会话→3D→路线列表；画布返回对话；所有 3D 节点可进入且走动不建内容 |
| 身份/设置 | OAuth 失败不假成功；本地原型不冒充登录；删除假协议/隐私链接；删除密度、动效、默认深度；身份/来源无后端时不伪造 |
| 真实 provider | 缺配置、鉴权、限流、超时、无效结构、空结果分别处理；生产无 mock fallback；浏览器不含 provider secret |

## 结构合同测试

- 为 R1、R2、R3、R3b、R4、R5、L0a、L0b、G1、G2、A1、A2、A3、N1、N2 各自测试输入最小集、合法输出、额外字段、缺字段和错误引用 ID。
- R4 验证 nodeId 唯一、edge 端点存在、路线能表达顺序/分叉/汇合，且 R2 的动态中文键对象不能通过 R4 validator。
- G1 验证 host/siblings/predecessors/successors 分组和边端点在压缩后保持；G2 验证只含当前对话且顺序/role/messageId 保持。
- A2/N2 验证 authorId、evidenceId、链接的绑定不会因去重或压缩串人；A2 不能选输入外作者。
- 压缩测试必须证明数据库/原始对象不被覆盖，并证明没有「压缩后仍太长」错误终态。

## 未裁决部分的测试边界

普通 Chat、图文编排、首轮持久化/并发、附件生命周期、异步恢复、思考深度冻结点、博主网络拓扑和 3D 位置 schema 尚未裁决。可以测试当前 fail-closed 或原型行为，但测试名和断言必须写明它不是最终产品合同，不能借测试冻结方案。

## 按改动范围验证

- 文档/规则：运行规则结构、链接和一致性检查，以及现有 `check:architecture`、`check:product-invariants` 中相关门禁。
- Agent/合同/后端：运行对应 unit、schema/contract、编排失败测试和真实 provider live gate；live gate 与 deterministic test 分开报告。
- UI：运行 typecheck、unit、build，并用真实浏览器覆盖受影响状态、键盘、焦点、低/窄视口和返回链。
- 3D/画布：除结构 validator 外，验证可进入性、视图恢复、批注显示/关闭和不会产生隐式领域写入。

## Definition of Done

一个切片只有在以下条件同时满足时才可说完成：

1. 行为与两份重构文档一致，没有用旧源码或旧测试替换目标。
2. Agent 上下文、预算压缩、系统提示词、输出 schema 与下游 ID 校验是同一合同。
3. 用户请求使用真实 provider；任何失败都明确，不回退 mock、fixture 或预写内容。
4. 成功、空结果、provider 失败和部分成功均有可重复证据，且没有写入不该产生的路线、图、批注或作者关系。
5. UI、后端和结构测试通过；文档只报告实际达到的成熟度。
6. 尚未裁决的实现细节仍明确标记为未裁决，没有被代码、测试或规则偷偷冻结。

只有现场源码、当次命令和真实运行证据都满足相应切片，才能标为 implemented/integrated；文件存在或测试关键词命中不能证明 production-ready。
