---
description: "定义与两份重构文档一致的测试矩阵、证据边界和完成标准。"
globs: "src/**,apps/**,packages/**,server/**,tests/**,catalog/**,qa/**,package.json,tsconfig*.json,vite.config.*,eslint.config.*,playwright.config.*"
alwaysApply: false
---

# 测试与验收

以[最新目标](../../as-implemented-logic.md)和[Agent 合同](../../agent-specs.md)为准。旧测试只证明旧链；变更后的行为测试必须覆盖实际新入口，不用源码关键词替代集成验收。

| 范围 | 必须覆盖 |
| --- | --- |
| 路线 | 4–5 问按角度两路搜索、三轮上限、已答题刷新仍可见、全答自动 R4、renderer 校验、恢复只重做未完成步骤、发布不建图 |
| 学习 | 三路搜索收齐再直答、完整总结、筛离题、首轮恢复、每段单依据、单父树、新对话清空但保留树 |
| 作者 | 1–3 新作者、证据回查、真实零位才直答、provider 失败不能冒充零位、自动发现不加分、显式使用幂等计数、反馈撤销和材料回流、刘看山不入网 |
| 数据 | 所有者隔离、并发重复提交、数据库重开、租约接管、迟到提交、原子回滚、卡片三方编辑合并与冲突 |
| 上下文 | 真实窗口减输出/余量、每块都被读取、ID/否定/来源保留、原文不变、失败不截断、摘要缓存按所有者隔离、深度固定 |
| 外部调用 | 限流恢复、认证不可盲重试、半截流/reasoning/length 不成功、同 Agent 同深度结构修复、引用检查后才落库 |
| UI | 真实文章/图/文档、浮动提问、编辑定位、添加/颜色、草稿、断线刷新、历史、新对话、窄屏和键盘焦点 |
| 部署 | 真实 PostgreSQL 两进程领取、备份恢复、生产鉴权/CSRF、容器启动、健康检查与负载；配置检查不等于 provider 可用性 |

运行 npm run check、npm test、npm run build。持久链测试是 npm run test:durable；真实 PostgreSQL 用单独 threadpeak_test 库运行 npm run test:postgres；真实 provider 和浏览器证据另记日期、范围和结果。不得把本地 PGlite 或 mock 测试称作 PostgreSQL/live gate。

文档检查包括十条规则路由、frontmatter、非空 globs、少于 500 行、本地链接、围栏、trailing whitespace、merge marker。已归档旧合同不能用来覆盖当前目标。测试失败若源于旧目标，要同步替换断言并保留新行为覆盖，不能删测试来隐藏回归。

只有生产身份接通、迁移/备份/恢复、真实用户流、压缩质量、性能与观测均达到验收指标后，才能声明 production-ready；本地通过次数不是永久事实。
