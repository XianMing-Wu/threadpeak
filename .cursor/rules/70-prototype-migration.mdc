---
description: "指导把当前错误原型迁移到两个重构文档定义的目标流程。"
globs: "src/**,package.json,tsconfig*.json,vite.config.*,README.md,catalog/**"
alwaysApply: false
---

# 当前原型迁移规则

[`as-implemented-logic.md`](../../as-implemented-logic.md) 第 1 节记录的是现场源码，第 2 节和第 4.1 节才是已确认目标；[`agent-specs.md`](../../agent-specs.md) 是 Agent 合同。源码大量错误，不能反向覆盖文档，也不能把现状表中的 timer、fixture、storage 或旧 endpoint 改名后当成目标实现。

## 现状到目标的替换表

| 旧实现或旧说法 | 重构目标 |
| --- | --- |
| 一次模型直接产一条路线 | R1 拆 4–5 个检索问题 → R-S 最多 2 路并发搜索（问法空格拼串）→ R2 动态探索 → R3/R3b 最多 3 轮 → R4 稳定 ID/显式边 → 校验后发布 |
| R2 的中文动态键对象直接拿去渲染路线 | R2 只用于探索；只有 R4 的节点数组、稳定 ID 和显式边是最终路线 JSON |
| 路线问答后用本地剪枝得最终路线 | 每轮答案进入 R3b 或 R4 的已定义上下文；当前题组选完后自动进 R4，不得用旧的纯 TypeScript prune 或「生成完整路径」按钮代替 Agent |
| 发布路线或 3D preload 时建知识 | 路线只发布路线；第一次进入概念才运行 L0 并建立唯一图/根 |
| 首次进入走普通回答，再单独 bootstrap graph | L0a 三路直答（同时在飞最多 2 路）→ L0b 整理；首轮回复与确定性根是一个成功状态 |
| 本机有知识记录就能点进画布，没有图就显示 GraphSurgeon/growGraph 失败页 | 没有已 settle 的 graph/root 时不出现我的知识脉络卡片，也不能点进失败说明页 |
| 根节点另调模型，或根失败但保留「首次回复成功」 | 根不调模型；L0b 成功与根落成不可拆成两个产品终态 |
| 再次进入或新对话重生成首次回复 | 永久复用同一 canonical 首次回复与唯一根 |
| 后续回答完成后串行调用旧 growGraph/A1 KnowledgeDraft | 同时运行 G1 与 G2；按两者结果矩阵决定是否新增卡 |
| 点击节点等于选择宿主 | 只有显式引用决定宿主；未引用默认最近一次成功 LLM 回复卡 |
| 选择用户问题本身作为结构宿主 | 映射到那条问题对应的 LLM 回复卡 |
| 问博主直接找一条 query、固定作者、先查本地网络或把回答做结构节点 | 划选 + 问题 → A1 拆 2–3 问 → A-S 最多 2 路并发知乎搜索（多问法空格拼串）→ A2 选 1–2 位真实作者；零位才 A3 刘看山；结果只做批注 |
| 博主网络只要命中一人就停止，或网络不足直接报错 | N0 先高权后低权，仍不足 3 人才 N1/N-S/N2 查知乎补剩余位置 |
| 知乎候选不足时凑够 3 人 | 返回全部有效候选，不重复、不虚构 |
| 搜索得到的知乎作者不入网，或所有作者一律同权重 | 问博主选中的真实作者写高权重；搜索补位的新作者写低权重 |
| 刘看山作为固定作者或网络节点 | 刘看山只是零作者时的直达回退，永不创建 AuthorIdentity/关系 |
| 首页附件按钮只报错，或误加首页问博主快捷 | 首页无问博主入口；问博主只有划选后出现；附件按钮只接受 pdf/md/txt |
| 首页普通发送走知乎检索 + 刘看山 prototype | 首页非路线普通发送走 R5：该 Chat 全文和只属于该 Chat 的附件，达到或超过 500k 才压缩 |
| Chat 空打开用专用失败页或预写问题 | 没有发送上下文时进入独立 404 |
| 首页附件跟进学习会话/画布 | 附件只在首页；路线把相关摘要与 sourceId 写入概念详细描述，学习阶段不带原件 |
| 深度思考在首页、学习页、画布全局共用 | 学习页和画布同步；首页状态不带入学习 |
| 设置保留密度、动效、默认思考深度 | 删除这三项，只保留已确认设置 |
| 图文用 timer/fixture 冒充生成 | 未接通时明确失败 |
| JSON 解析失败立刻当该 Agent 失败，或另写循环修复 Agent | 同一 Agent、同一系统提示词和同一输出结构自我修复；不得启发式补成功，也不得把校验原文展示给用户 |
| 3D 边控制解锁或写学习进度 | 边只推荐流转；节点都能进入；走动不创建内容 |

## 迁移顺序

1. 先建立 `agent-specs.md` 所列上下文 builder、预算器、压缩器与结构校验，保证所有 Agent 共用同一套 500k/300k 规则。
2. 纵向替换路线：R1/R-S/R2/R3/R3b/R4、renderer 校验、发布、3D；删除 path-lab 产品入口和旧单次生成链。
3. 替换概念首次进入：L0a/L0b 与确定性唯一图/根统一提交；删除普通回答冒充首轮和独立 bootstrap 状态。
4. 替换学习追问：显式宿主解析、G1/G2 并发、结果汇合与同一会话/画布投影；删除点击宿主和旧串行长图链。
5. 替换问博主和博主搜索：A1–A3、N0–N2、作者证据 ID 校验及高/低权重写入；删除固定作者和旧本地 Graph RAG 真相。
6. 最后清理页面 timer、fixture、旧 localStorage/sessionStorage 事实链、错误设置项、假协议链接和预写成功内容。

这只是依赖顺序，不等于要求一次性大改。每个切片先完成对应合同和失败测试，再替换消费方；旧链仍存在时必须明确标为 prototype，不能与新链共同写同一事实。

## 不得在迁移中自行补完

- 真正的图文生成管线。
- R5 失败重试、跨重启历史落盘和取消/超时状态机。
- 首轮持久化表、唯一作用域、跨重启事务和并发策略。
- 附件数量/大小、解析失败、删除及 sourceId 生命周期。
- 取消、超时、刷新恢复、重复提交和迟到响应。
- 思考深度冻结点、芯片最终文案。
- 3D 位置数据格式与持久化、博主网络跨重启存储。

需要实现这些内容时，先让产品文档明确，不得从旧源码、旧算法文档或本规则猜答案。

## 看到即阻断

- 生产请求走 fixture、固定作者、预写回复或 timer 假成功。
- 路线发布触发知识图，或首次回复之前出现根节点。
- 没有已 settle 的根时仍展示可点的我的知识脉络卡片，或打开独立 bootstrap 失败页。
- 把 R2 探索对象当成 R4 路线文档。
- 压缩后超预算直接报错，或用截断破坏 ID/边/作者证据对应关系。
- 点击节点改变追问宿主，或没有用户问题就发学习追问。
- G2 失败仍写图；G1 失败却伪造结构卡。
- A2 provider 失败被当成零候选并触发刘看山。
- N0 投影不可用被当成网络零命中并绕到知乎。
- 作者搜索只查网络第一层就结束，或候选不足时虚构补满。
- 把作者回答/刘看山批注写成结构节点。
- 浏览器直接持有密钥或调用知乎/LLM provider。
- 用 storage、布局位置或消息文本作为路线、知识图、会话或作者网络的权威事实。

## 本地存储边界

两个文档没有裁决新的浏览器持久化合同。现有 storage key 只能被视为待迁移现场；不得在重构规则中列出一份新的长期白名单并把它冒充为产品决定。确需新增本地状态时，只能保存可丢弃的界面偏好或未提交输入，且不能覆盖服务端事实。
